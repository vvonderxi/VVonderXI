# VVonderXI , HANDOVER, 2026-09-12

**This replaces `docs/HANDOVER_2026-09-04.md`, which is 106 commits stale.** Read this, then
read `CLAUDE.md` in full. `CLAUDE.md` remains the source of truth and wins on any conflict;
this file is the routing document.

**EVERY FIGURE HERE WAS MEASURED FROM THE REPO OR THE DATABASE ON 2026-09-12.** Where a claim
could only come from a chat transcript, it is marked `[UNVERIFIED, chat-sourced]` or left out.

---

## 1. VERIFY WHERE YOU ARE, BEFORE ANYTHING ELSE

```
branch            redesign-compare
expected HEAD     docs: the assists honour was a maximum over three cards, and card
                  resolution comes first          , the commit carrying THIS file
HEAD's parent     02bfae8   docs: record the lineage test and four rulings...
origin            2572073   , FIVE commits behind HEAD
unpushed          e059eea, ea98f9f, 1e8b059, 02bfae8, + HEAD
```

**Terminal C, first command every session: `git log --oneline -4`.** If HEAD's SUBJECT is not the
line above, STOP and find out why before editing anything , someone has pushed, reset, or continued
past this document, and the sections below describe a tree that no longer exists.

**NOTHING HERE HAS BEEN PUSHED. Lucas pushes, always, from Terminal C.**

**[THE HASH IS DELIBERATELY GONE FROM THE `expected HEAD` LINE, AND THIS IS THE THIRD TIME IN ONE
DAY THIS BLOCK HAS BEEN WRONG , it expected `e09aa4b`, then `c40e3c0`, and both fell behind within
hours.]** The third time was structural rather than careless: **a commit cannot name its own hash.**
Writing the hash in, committing, then correcting it and amending produces a file naming the
PRE-AMEND commit, every time. **The subject survives an amend and the hash does not**, so the
subject is what this line carries. **The PARENT hash is safe to write and is the useful check** ,
`git log --oneline -2` settles both at once.

---

## 0. THE DATABASE WAS WRITTEN TO TODAY. READ THIS BEFORE ANY HONOURS OR ASSISTS WORK.

**This is the only section describing changes to DATA rather than to the repo, which is why it is
first.** Everything here is applied and the matview has been refreshed, four times across the day,
in the Supabase SQL editor , **which is Lucas's lane and the only working route.**

**STATE AS OF THIS WRITING, MEASURED AGAINST THE LIVE DATABASE AT THE END OF THE SESSION:**

    honours rows                 624      (was 631 at the start of the day)
    top_assists rows             113      (was 120: 11 deleted, 4 sourced and written)
    cards with h_top_assists     113      (matview agrees with the table)
    rows marked sourced_leader_only_verified   4
    player_card_mv rows       57,055      (unchanged, verified intact after each refresh)

**THE MATVIEW IS CURRENT. Four refreshes were run in the Supabase SQL editor across the day**, the
last of them after the batch-2 write; `h_top_assists` was confirmed at **113** by a REST count
against `player_card_mv`, not read off the results pane. The `exec_sql` route still dies at 8.5s on
the statement timeout, re-confirmed, so the SQL editor is the only route and it is Lucas's lane.

**WHAT CHANGED AND WHY, IN ONE PARAGRAPH EACH:**
1. **Eleven `top_assists` rows are gone.** Ten were computed from an assists column that is 99%+
   null before 2015, so the "league leader" was the maximum of a handful of populated cards , Van
   Persie held the PL 2011/12 assists honour on **9**, chosen from **three** populated cards in a
   **390-card** league-season. The eleventh is the PRT 2020 pair, replaced (below). Rollback
   captures in `migrations/top_assists_sparse_2026-09-12/` and
   `migrations/top_assists_prt2020_2026-09-12/`.
2. **`PRT 2020` changed hands.** Grimaldo and Nunez (both 9) deleted, **Taremi (10) written**. This
   one was NOT an artefact , Taremi's card arrived in the 2026-07-23 ingestion recovery, nineteen
   days after the honours were computed on 2026-07-04.
3. **`L1 2023` is deliberately EMPTY and must stay that way.** See SS 0.1.
4. **Nothing else drifted.** All 93 live league-seasons were recomputed and compared: **92 exact,
   0 value drift, 1 winner change.** The exposure was bounded at 69 league-seasons (those touched
   by the 780 recovery cards) and exactly one changed. **This question is closed with a number.**
5. **FOUR `top_assists` rows were WRITTEN from external sourcing**, all carrying
   `source = sourced_leader_only_verified`, all delete-before-insert with rollback capture in
   `migrations/top_assists_sourced_2026-09-12/`:

       id=635  PL 2011/12  David Silva     15a  api 634    card 136016  Man City   phase 1
       id=636  PL 2014/15  Cesc Fabregas   18a  api 108    card 134878  Chelsea    phase 1
       id=637  LL 2014/15  L. Messi        18a  api 154    card 141853  Barcelona  batch 2
       id=638  SA 2012/13  M. Hamsik       14a  api 12994  card 150038  Napoli     batch 2

   **Every one cleared BOTH gates: two genuinely independent lineages, and a resolvable card.**
   Phase 1 ran 7 two-read league-seasons and wrote 2, a **71% hold rate**. Phase 2 was NOT run and
   is closed as specified , see SS 0.4.
6. **`write_positions3.js` no longer writes assists.** It was writing 28 CCC assist values as a
   side effect of a POSITION script, which is how hand-researched assists ended up with no
   provenance record. They now live in `scripts/enrichment/write_assists_ccc.js` with a full
   header, verified idempotent (`filled 0 / already-had 28`). **No value changed; only the
   ownership did.**

### 0.0 TWO APPROVED JOBS. ONE IS BLOCKED ON LUCAS, ONE IS PART-DONE.

**[THIS HEADING USED TO SAY "SCOPED AND UNSTARTED , NEITHER HAS WRITTEN ANYTHING" AND THAT IS NO
LONGER TRUE OF B.]** Read the spec before touching either.

**A. SQUAD NUMBERS, variant A , BLOCKED ON LUCAS, AND DO NOT RAISE IT AGAIN UNTIL HE BRINGS IT.**
`LAUNCH_STAGE.md` plus the scope in this session's log. Scoped, demoed, approved in principle.
**460 top-10 club-seasons, 8,905 cards, 8,771 blank shields, 43.7% of the 20,063 blank.** Three
things must land in the SAME change or the backfill is erased: the `league_standings` alias pass
(16 explicit mappings, never fuzzy), the Pattern A writers changed from `ON CONFLICT DO NOTHING`
to an explicit update-or-insert, and the importers taught not to null a shirt number they do not
know. **And note it is EIGHT leagues, not nine** , see the `league_standings` entry in
`DATA_DEFECTS.md`.

**THE BLOCKER IS ONE UNANSWERED PRODUCT QUESTION, NOT A BUILD GAP: does a card that today shows a
deliberately BLANK shield get a number?** It affects ~8,771 card faces. **Lucas was asked three
times on 2026-09-12 and the answer never came** , the placeholder came back unfilled twice. **His
instruction, verbatim in intent: leave it blocked and stop asking, he will bring it when he
decides.** Do not re-ask, do not build a variant "to have it ready", and do not write a default.
**A blocked job with a named blocker is a correct state.**

**B. TOP_ASSISTS WINNER SOURCING , NO LONGER UNSTARTED. FOUR OF 46 ARE WRITTEN AND THE JOB IS
NOW CONSTRAINED RATHER THAN OPEN.** Full spec in **`docs/TOP_ASSISTS_SOURCING_SPEC.md`**, which was
rewritten twice today and now carries a SS 0 and a SS 10 that did not exist this morning.
- **Transfermarkt is PRIMARY, not Wikipedia.** The original ordering was wrong: Wikipedia carries a
  "Top assists" section for **2 of 20** checked league-seasons. Tested and rejected with reasons:
  the La Liga archive (current season only), the Bundesliga archive (oldest 2019/20), FBref (not
  independent of the column it would be correcting).
- **The confirmation bar is LINEAGE, not source count** (SS 9). Opta-derived plus Opta-derived is
  one read. An AWARD is never a read , see SS 2's Playmaker entry.
- **CARD RESOLUTION IS A PRECONDITION, not a final check** (SS 0). Three named league-seasons are
  CLOSED rather than held because the leader has no card at all: **Nani PL 2010/11, Juan Mata PL
  2012/13, Hakim Ziyech ERE 2014/15.** The 300-minute floor explains none of the three , their
  squads carry 14 to 21 cards down to 313 minutes. They are source-absent individuals, the Rooney
  and Gerrard class, and a re-ingest will not recover them.
- **Phase 2 is CLOSED as specified**, on a finding rather than on the 71% hold rate. See SS 0.4.
- **STILL OUTSTANDING: the pill-fit check on a REAL card at `--cw` 132 two-up, not in a harness.**
  The fallback is pre-approved.
- **FOR THE NEXT EXTERNAL PASS: commission card resolution FIRST.** Roughly a fifth of the 45-row
  Fable report's effort went into league-seasons that could never have produced a row.

### 0.1 THREE THINGS A FUTURE SESSION WILL BE TEMPTED TO "FIX". DO NOT.

- **`L1 2023` has no `top_assists` row and that is the honest state.** The computation offers a
  four-way tie on 8. A four-way tie at that ceiling in a top-five league is a coverage tell, not a
  result. It passes the coverage gate at 84.3%, so the gate cannot catch it. **Writing four
  honours to record our own uncertainty would assert four claims where reality has one.**
- **The 28 hand-researched assist values in `write_assists_ccc.js` stay.** They are real research,
  the marquee batch was FBref-verified, none holds an honour. **Reverting them would delete real
  data to fix a routing problem**, and would leave 28 cards scored on an assumed zero.
- **A rank-keyed selection on `league_standings` covers EIGHT leagues, not nine, and shrinks
  silently.** Belgium holds 4-6 rows per season for 13 of 16 seasons (the playoff-group table),
  and TR 2010 is absent. A `rank <= 10` query returns fewer rows and succeeds, so the job reports
  a clean run. **Assert the row count reaches N before taking the top N.**
- **The `MIN_COVERAGE = 0.25` gate is NOT lax and must not be tightened.** It sits in a measured
  empty region (ten artefact rows at 0.5-1.9%, then nothing until 45%). **At 0.60 it refuses 78 of
  120 correct rows**, silently, because a withheld season prints as UNRESOLVED rather than as an
  error. A healthy league-season is only 50-70% populated, by design, because of the importer's
  300-minute floor.

### 0.2 THE FINDING WITH THE LONGEST REACH, IF YOU READ ONLY ONE THING

**A FILL PRIORITISED BY PROMINENCE MAKES EVERY COMPUTED EXTREMUM LOOK CORRECT.** Pre-2015 assists
were filled only for marquee cards , **117 cards, median rt 88, 89.7% at rt>=85, against a
population median of 42**. So the maximum over the populated set was a star by construction, and
the false honours landed on Messi, Van Persie, Suarez, Giroud and Nene. **A sparse maximum over a
random sample looks obviously wrong and gets caught. Over a sample selected for fame it looks
exactly like the right answer**, which is why the defect survived from July to September.
**This applies to any selective fill of any field, not just assists.**

### 0.3 THE ASSISTS BACKFILL IS CLOSED, PERMANENTLY, AND HERE IS THE ONE FACT THAT CLOSES IT

**The percentile pools are NOT partitioned by season.** Eleven `PARTITION BY` clauses in the live
view, none carrying `season_year`; `pos_pct` partitions on `COALESCE(pool, pos)` alone. **So every
pool mixes 2010 with 2025, and ANY era-bounded fill is a partial fill of every pool** , measured at
3.1x to 8.3x worse per card than a complete one. The real scope is **31,040 cards across 2,422 of
2,740 club-seasons, 88% of the database**, not an era repair. **Top ten clubs, fifty players, one
era, one league: all the same shape. Do not re-propose a subset.** Full record, with the three
simulated treatments and their band-crossing costs, in `DATA_DEFECTS.md`.

### 0.4 PHASE 2 OF THE ASSISTS SOURCING IS CLOSED, AND NOT BECAUSE THE HOLD RATE WAS BAD

**"Who won the assist crown" is not a single fact independent of who is counting.** Measured on
**5 of 5** disagreements: **Transfermarkt is EQUAL OR HIGHER than the official record, never
lower.** A recording error scatters both ways; this does not.

**AND THE RULE CANNOT BE NAMED, WHICH IS WORSE THAN AN ALTERNATIVE DEFINITION.** Transfermarkt
publishes no assist definition anywhere reachable, and its own data-administration guide explains
why: assists are entered **per match by volunteer scouts** and aggregated automatically. **There is
no offset to apply, because there is no rule** , a documented alternative convention would have
rescued phase 2, an undocumented per-scout judgement does not.

**[ADDED AT THE END OF THE SESSION] THE SIGNATURE NOW HAS A SECOND INSTANCE ON A SECOND PROVIDER,
AND THAT STRENGTHENS THE CLOSURE.** `PL 2013/14`: **worldfootball gives Gerrard 14 and Suarez 13**
against the official archive's **Gerrard 13, Suarez 12** , one higher on BOTH, so a level shift and
not a re-ordering. **Two providers agreeing with each other and disagreeing with the official record
in the same direction are NOT two independent reads**, which is exactly the lineage trap the spec's
SS 9 exists to catch. **This reads as an ecosystem-wide definitional split between the official/Opta
count and the wider football-data world, not one provider's quirk. DO NOT apply a -1 correction** ,
the LL 2010/11 row is +3. Full record in `DATA_DEFECTS.md`.

### 0.5 THE 45-ROW EXTERNAL REPORT PRODUCED TWO ROWS, AND THE AUDIT IS WORTH MORE THAN THE ROWS

A four-step audit of an external sourcing report: **11 rows arrived marked CONFIRMED, 4 were
genuinely two-lineage, 2 were writable.** The three things that killed the other nine, in order of
cost:
1. **Card resolution** , the leader has no card. Unfixable by research.
2. **One lineage wearing two names** , an award plus the official archive it is adjudicated from,
   or two Opta-derived reads.
3. **A `CONFIRMED` label contradicted by the row's own notes.** The evidence governs, not the
   status column.

**AND A GENERAL RESEARCH RULE CAME OUT OF IT , AN AWARD NAMED FOR A METRIC IS NOT THE METRIC.** The
report cited the Premier League Playmaker award as evidence for the assists table. The award IS
given for most assists, which is precisely why the conflation is invisible , but it is adjudicated
from one provider's count, so award-plus-archive is one reading twice. **Golden Boot is not the
goals table. An honour is downstream of a measurement, never a second instance of it.**

**VERCEL'S PRODUCTION BRANCH IS `coming-soon`, NOT `vvonderxi_BIGGER` , RE-READ IN THE DASHBOARD
2026-09-12, from Settings > Environments, not inferred.** Production's Branch Tracking is
`coming-soon` and it serves **vvonderxi.com plus four more domains**. `vvonderxi_BIGGER` builds as
a preview and **deploys NOTHING**. **That setting is the launch gate, not the merge.**

**AND THE FIRST MERGE HAS ALREADY HAPPENED , THE "PENDING FAST-FORWARD" RECORD IS SUPERSEDED.**
`4c8ce8a`, 2026-09-06, *"Merge redesign-compare into vvonderxi_BIGGER , the platform returns,
adopting the branch's tree"*, parents `afb9970` and `32b19da`. **`vvonderxi_BIGGER` carries the
platform now** , card.html, compare.html, vv-core.js and index.html are all on it.

**Measured after a fresh fetch on 2026-09-12:**
- merge-base **`32b19dab`** , NOT `5bdbadb` and NOT `775095f`, both of which appear in older docs
- `origin/vvonderxi_BIGGER` = `4c8ce8a`, **11 ahead** (ten holding-page commits plus the merge)
- `origin/redesign-compare` = **85 ahead**
- **`git merge-tree` reports ZERO conflict blocks** over 3.7 MB of output
- **ZERO files exist on BIGGER that are absent from redesign**, so the merge deletes nothing ,
  which is the precise danger `4c8ce8a`'s own message records from the rejected naive three-way
  (22 conflicts and 183 files silently dropped)

**So what remains is 85 commits of DRIFT, not an atomic first merge.** BIGGER has no
`vv-margin.js` and zero `cabinetLayer` against redesign's two, which is exactly that drift.
**Do not check for "0-ahead" before merging** , that is the superseded shape. Check that
merge-tree is still clean and that no file exists on BIGGER and not on redesign.

**[CLOSED AT THE END OF THE SESSION. THIS PARAGRAPH SAID THE MATVIEW WAS BEHIND THE BASE TABLE AND
IT IS NOT ANY MORE.]** The eight 25/26 position corrections are **LIVE** , `player_positions` and
`player_card_mv.position_pool` now agree on **8 of 8**, verified by query. The refreshes run for
the honours work carried the position batch with them.

**THAT IS THE PATTERN WORTH TAKING FROM IT: a matview refresh run for ONE job lands EVERY pending
write, and nothing announces it.** After any refresh, check what else was sitting unrefreshed.

**AND THE DIFF THAT WAS OWED IS DONE , the simulator is trustworthy on counts and not on names.**
Measured against `before.json`, all 57,055 cards: **113 rt movers (predicted 116), 6 band crossings
(predicted 6), 8 pool changes, median |delta| 1, max 3.** The four RECLASSIFIED crossings were
predicted exactly, by card and by value. The two UNTOUCHED ripple crossings were right in count and
direction and **wrong in identity** , predicted Vardy 16/17 and Alcacer 18/19, actual **Perisic
16/17 and Lacazette 23/24**, all four 84 -> 85. **Quote `pool_change_sim.js` for its COUNTS, never
its NAMES for ripple cards** , which of several tied neighbours re-ranks is not stable. Full table
in `migrations/positions_2526_2026-09-11/README.md`.

---

## 2. WHAT SHIPPED SINCE 2026-09-04

106 commits, 57 files, +9,083 / -948. Grouped by area, oldest first within each group.

### Goalkeepers , Fallback C, and the removal of every keeper scalar
The largest single change. A keeper card no longer carries a number anywhere.
- `794d655` pre-register the mark threshold before any mark is counted
- `0227eef` `7a9bbcd` `b8990e9` `da5471d` shadow scorer, shots-faced floor sweep, mark scorer,
  and the negative results they produced
- `ceaf4a9` the keeper fields were captured but never read, and four do not hold what their
  names say
- `13b4081` the keeper score has no goalkeeping in it (minutes and league, nothing else)
- `516ba3b` **Fallback C, the shipping spec, authored in the Fable session** , `docs/KEEPER_FALLBACK_C_SPEC.md`
- `32a8fb0` `de6bea4` `081feb6` Fallback C: the card, then keepers leave every rt list, then
  what the model receives about a keeper
- `86cd73a` `3f7fe9d` the card face carries no score, and neither does the share caption
- `5d112af` `c023dac` no verdict when either side is a keeper, and the share affordance goes with it
- `470b985` `d6df6e8` field names never reach the reader; penalties saved has no denominator
- `1af2e75` two panels instead of a ranking
- `a6054c9` `eea09e2` the Playbook's keeper ladder is drawn, and the section demonstrates the rate

### Engine , the defensive signal
- `69d7b48` `sig = def_share_pct` alone, prepared and disclosed, NOT yet applied
- `9ae9985` **APPLIED, and the full-population diff matched the simulation exactly** , 10,770
  of 50,269 outfield cards moved, median 2, max +/-7, **zero band crossings**
- `6d1e3dc` `72832d5` the blend split measured facet by facet, and the null policy's obvious
  fix measured and rejected

### The radar
- `21204e5` **percentile within position pool**, replacing one denominator for eight pools.
  `RADAR_POOL_REF` in `vv-core.js` is an EMBEDDED SNAPSHOT, regenerated by
  `scripts/gen-radar-ref.js`.

### Compare , the margin class and Path B
- `67da01d` the outfield bands, and which of them the evidence can separate (the separability audit)
- `5236500` `e1ae85b` the audit reproduces independently; the checked-in scripts run
- `12d1ff0` **the margin class** , Compare stops crowning what the engine cannot separate
- `8339a8b` **Path B** , the model decides the winner for pairs inside the margin, and the
  score becomes evidence rather than the verdict. The server verifies the returned winner is
  one of the two cards and nulls it otherwise (`resolveWinnerId`, exercised by
  `scripts/pathb_guard_test.js`, 15 cases).
- `cc801ff` rule B names the constraint it actually imposes

### The AI payload
- `e8620d7` the Fable payload design brief, verbatim
- `c3021be` the cheap tier , honours, glosses, stage flags, denominators, radar
- `f2a74c7` `f524160` `110aaea` `3eaee92` no quality claim may be read off rt for CB, FB or CDM;
  a gated card's score may not be named; the band travels with the score
- `351d64f` a naming contract, and four things the prompt was teaching wrong
- `b945f80` the verdict cache version tracks the payload schema, not just the prompt

### Tags and filters
- `b585eee` `fb13fd2` Peak, Breakout and The Standard reach every surface, and the three
  Career-Stage chips return results , **computed in `player_card_view`, not in JS**
- `a4f4e08` **six honour types filter server-side** (the `h_*` columns from the 09-04 matview swap)
- `9d3f980` World Cup renders as a career honour and the seventh chip goes live
  , **NOTE: superseded three days later, see the cabinet below**
- `e897b97` The Standard prints as career-legged

### Positions , the 25/26 batch
- `0333de9` **eight 25/26 corrections written, eight held.** Two independent Fable passes over
  all 67 unverified 25/26 cards at rt >= 80; pass agreement 65 of 67; the 8 that were high
  confidence in BOTH are written. Record: `migrations/positions_2526_2026-09-11/README.md`,
  decisions and both passes' evidence in `research/pos2526_verified/`.
  **[UPDATED 2026-09-12 , ALL EIGHT ARE NOW LIVE, 8 of 8 verified against the matview.]** The
  before/after diff is done: **113 rt movers, 6 band crossings, median |delta| 1, max 3.**
  **The radar reference and the margin table still have NOT been regenerated and are now DUE
  rather than blocked** , they read the matview, which has moved. See section 4.

### Honours , 2025
Measured today: `honours` holds **631 rows**, of which **31 carry `season_year` 2025**
, 18 `wikipedia_ccc`, 13 `computed`; by type **golden_boot 10, top_assists 11,
league_champion 9, ucl_winner 1**. A UNIQUE index `honours_one_per_award` is live.
**THERE ARE ZERO `player_of_season` ROWS FOR 2025.** Any note claiming otherwise is wrong.

### Honours , the `top_assists` correction and the sourcing run (2026-09-12, the day's main work)
- `e059eea` `ea98f9f` `1e8b059` `02bfae8` plus the batch-2 write. **`honours` ends the day at 624,
  `top_assists` at 113 of 144 possible league-seasons**, 4 of them externally sourced.
- **The defect: `top_assists` was computed from an assists column that is 99%+ null before 2015.**
  Van Persie held PL 2011/12 on **9 assists, the maximum of THREE populated cards in a 390-card
  league-season.** Corrected by DELETION (11 rows), which is why the sourcing job exists.
- **The write path is `scripts/enrichment/honours/top_assists_write.js`.** `MIN = 9` is gone,
  replaced by `MIN_COVERAGE = 0.25`; nulls are EXCLUDED rather than read as zero; ties are carried
  as a list and every tied player is written. `compute_top_assists.js` is its READ-ONLY twin , **an
  audit of the twin is not an audit of the write path**, and reading the wrong one produced a false
  claim in this session that 19 players held no honour they had earned.
- **`scripts/enrichment/write_assists_ccc.js` is NEW** and owns the 28 CCC assist values that
  `write_positions3.js` had been writing as a side effect of a POSITION script. Full provenance
  header; verified idempotent (`filled 0 / already-had 28`). No value changed, only the ownership.

### Playbook , the league strip states volume once
- `ed704f5` the league strip carries **"every league here carries more than 5,000 season cards"**
  as a single floor sentence rather than a count per pill. Scheme A of three demoed; rendered and
  measured before it shipped. The 18 per-league figures are kept in a code comment beside it so the
  floor can be re-checked without re-querying.

### The Cabinet
- `9d7f49c` the Fable ruling, verbatim , `docs/CABINET_SPEC.md`
- `29abbe9` the Cabinet ships, and **the World Cup career leg is retired.** The leg attached to
  every card a winner holds in BOTH directions: 496 pairs after the tournament, 91 on it, and
  **333 BEFORE it**, where a 2010 card of a 2014 winner printed a World Cup he had not won.
- `31a3c8b` the Cabinet becomes honour pills in its own foldable section after Wonder Tags
  , **a recorded DEPARTURE from the ruling, which says "glance, not face, not panel"**
- `e09aa4b` the `?v=` bump that `31a3c8b` should have carried
Notes and measurements: `docs/CABINET_SPEC_NOTES.md`.

### Playbook and card polish
`cf16099` career arc marks, `821ee88` honours cabinet as a grid of seven, `9c8ccef` Prestige
Tags, `c4556a4` keeper notes and the Wonder Tags threshold, `927bf20` the keeper ladder becomes
a rate scale, `6c67cd4` the arc fills its column and the nav is read from the DOM, `f8eac45`
five 390px defects, `8676036` the home page stops scrolling, `5446516` `8a1a49f` Wonder Tags
carry the strip's colour language, `0a4ad3e` the nine leagues as chips and a grid, `af3d95f`
England was a black flag.

### Documentation and QA
`39be690` five stale entries corrected and the GIN indexes re-created, `13b4081` `ceaf4a9`
keeper findings, `67da01d` the band separability audit, `2e66a68` the A/B identity colours as
an accepted exception, `6b983bd` `72962e1` band copy stops promising reproducibility the audit
denies, plus eight `docs(qa)` commits closing items in `QA_PASS.md`.

---

## 3. STALE , DO NOT ACT ON THESE

**Every entry below describes work that has since shipped or a fact that measurement has
falsified. They are listed with what the tree actually says.**

1. **`CLAUDE.md` §D line 456: "THE 75 CAP IS STATED ON THE CARD and must stay stated."**
   **FALSE, and acting on it would undo shipped work.** Fallback C removed every keeper scalar.
   `card.html` carries the comment "THE CAP SENTENCE IS RETIRED BY THE SPEC". A session
   restoring the cap sentence would be reversing `86cd73a` and `docs/KEEPER_FALLBACK_C_SPEC.md`.
2. **`CLAUDE.md` §C line 194: "Player-LEVEL accolades (career context ..., NOT season tags):
   World Cup Winner."** Superseded by `29abbe9`. The World Cup is now a SEASON honour on its own
   year plus a dated cabinet entry on every later card. The career leg no longer exists.
3. **`CLAUDE.md` §D FILTER FOLLOW-UP: "HONOURS ACTIVATION IS STRUCTURALLY BLOCKED , PostgREST
   cannot filter the matview by the separate honours table."** Shipped in `a4f4e08`; six honour
   types filter server-side off `h_ballon_dor`, `h_ucl_winner`, `h_league_champion`,
   `h_player_of_season`, `h_golden_boot`, `h_top_assists`.
4. **`CLAUDE.md` §D COMPARE item (g): "Drury prose renders as ONE long paragraph , fix at the
   PROMPT level."** Fixed at the prompt level: STYLE RULE 5 in `api/analyse.js` requires two to
   three short paragraphs, and `compare.html` splits on `\n\n` into separate `.vquote` elements.
5. **`CLAUDE.md` §A and §E: "honours 629 live."** Measured today: **631**.
6. **`CLAUDE.md` §D EM-DASH SWEEP: "the ONLY em/en dash left is `compare.html:1776`."** The line
   is now **2329**. The claim is still true in substance (it is the regex character class that
   matches dashes the model might emit, and deleting it would break the guard) but the line
   number has moved, as it did once before.
7. **`docs/HANDOVER_2026-09-04.md` career-stage section (lines ~177 to 189).** It describes the
   career-stage chips as a fix in flight and names a "latent defect found while measuring". That
   work closed in `b585eee` and `fb13fd2`; the rule now lives in `player_card_view` as columns
   on the matview, not in JS. **This is the section that has already sent one session to rebuild
   shipped work.** The whole file is superseded, not just this part.
8. **`CLAUDE.md` §A PROGRESS bars.** They predate all 106 commits. Treat as unmeasured.

**A stale-adjacent trap worth naming:** `9d3f980` ("World Cup renders as a career honour") is
itself a correct record of a change that was DELIBERATELY REVERSED three days later by
`29abbe9`. A `git log` search will find it and read as current. It is not.

---

## 4. OPEN WORK

### Needs Lucas , decisions, credentials, a device
- **[DONE , the refresh ran four times on 2026-09-12 and the matview is current.]** Kept because
  the ROUTE is the standing fact: **the refresh cannot be run from Claude Code.** §C's recorded
  `exec_sql` recipe dies at the 8s `statement_timeout` (measured twice, 8.6s), `set_config` is
  proven to take effect within the call, and `.env` holds no connection string so `psql` has no
  route. **It is Lucas's lane, in the Supabase SQL editor:**
  `set statement_timeout = '600s'; refresh materialized view player_card_mv;`
- **Pick a cabinet years treatment.** Three demoed at `_demo_cabyears.html`. **B and C use a
  count, which `docs/CABINET_SPEC.md` forbids by name.** A is the only spec-compliant option.
- **Decide whether the Cabinet section defaults open** when it has content.
- **API-Football key rotation** (exposed in a chat transcript, never committed) , §D step 5.
- **Ten QA_PASS Group C items** need a person.
- **`api/log.js`**: a deployed public endpoint writing session-linked comparison records with no
  caller on this branch. Delete or keep and disclose. Not urgent, not corrupting.

### Buildable now
- **The Playbook section explaining the Cabinet.** Recommended placement: inside the existing
  `#s-honours` section rather than its own, because the cabinet is a second view of the same
  seven honours. **[THAT "BLOCKER" WAS MEASURED ON 2026-09-12 AND IT IS NOT ONE , DO NOT RE-PROVE IT.]**
  This used to read: *"Blocker to settle first: `playbook.html` already styles `.cabwrap` under a
  comment reading THE TROPHY CABINET for the grid of honour TYPES, so the page would teach two
  cabinets."* **The word "cabinet" appears ZERO times in the playbook's visible text** , measured
  by stripping `<style>` and `<script>`, then all tags, and searching what remains. "TROPHY
  CABINET" is a **CSS comment**. The reader sees **"Honours"** with the label **"The silverware"**,
  on the section, the heading and the nav card alike. **There is no second cabinet on screen and
  no rename is needed.**
  **AND A RENAME WOULD BE THE ACTUAL RISK, WHICH IS WHY THIS IS RECORDED RATHER THAN DELETED.**
  `.cabwrap` is load-bearing for `vvFoldSections` (used twice, in the selector chain and the
  negative guard, with a comment saying renaming it "would have left Honours unfoldable with no
  error"), and `s-honours` is a mark key in `vv-marks.js`, where a rename without the matching key
  change renders a blank mark rather than an error, plus a shared `?v=` bump on five surfaces.
  **Rename nothing. If "Catalogue" is ever wanted on screen it is the `.label` line, copy only.** Two copy edits are required in the same pass: `HON_COPY["World Cup Winner"]`
  still states the retired career-leg rule, and the World Cup cell's `.hleg` still reads "every
  season of a career".
- **Compare has no Cabinet**, and the career-leg retirement costs it information meanwhile: its
  accolades panel shows the World Cup only on the tournament-year card.
- **The AI payload does not carry the cabinet.** `vvAIStats` still emits `leg` and
  `_hon.career` is now always empty. **`payloadRev` does NOT move for this** , only values
  changed, not the key set, so the fingerprint cannot see it.
- Trajectory redesign (Option 3), the picker pager, the fourteen verdict-tag marks.

### Blocked, and on what
- **[UNBLOCKED AND NOW DUE , NOT BLOCKED ANY MORE. DO THIS FIRST IN THE NEXT SESSION.]**
  **Radar reference and margin table regeneration.** The matview refreshed on 2026-09-12 and
  **113 cards' rt moved**, so both embedded snapshots are now stale against live data and
  **NOTHING WARNS YOU**. In this order:
  1. **`scripts/gen-radar-ref.js`** , regenerates `RADAR_POOL_REF` in `vv-core.js`. It reads the
     matview directly. Simulated impact negligible (medians move in the fourth decimal), but a
     stale embedded snapshot is a standing hazard and the point is that it stops being stale.
  2. **`scripts/separability/gen_margin_table.js`** , descends from the same matview via
     `pull_inputs.js` then `bootstrap_se.js`. **Path B's crowning gate reads per-card standard
     errors off these scores, so a moved rt moves a margin.**
  **Bump the shared `?v=` token on all five shipping surfaces in the same commit as any
  `vv-core.js` change** , found by `grep -l 'vv-core.js\|vv-marks.js' *.html`, never by a count.
  Cached verdicts need no action , `rt_a`/`rt_b` stamps make a moved card a cache miss.
- **The 2022 CAM collapse** (§E) , the classifier stopped emitting CAM at the 2022 boundary
  across all nine leagues (2021 25.4% of rows, 2022 0.5%, 2025 0.0%). **Blocked on a lineup
  re-pull**: the raw grid is discarded at import, so no fix can be dry-run from stored data.
  The importer must persist the grid first. **[ESTIMATE CORRECTED 2026-09-12, IT WAS ROUGHLY
  DOUBLE.] 33 league-seasons, not ~70** , counted from `player_positions` as the league-seasons
  from 2022 on whose CAM share is under 2%, **covering 16,105 rows**, out of 142 league-seasons
  in the table. **The call estimate scales with that count, so ~26,600 is about twice what is
  needed.** Count it again before spending anything; do not quote either number from memory.
  **THE TESTABLE HYPOTHESIS, RECORDED SO THE RE-PULL CAN CONFIRM OR KILL IT IN ONE PASS.**
  `scripts/import/import-positions-v2.js` lines 64 to 66: `depth = (row-defRow)/(fwdRow-defRow)`,
  `<= 0.45` gives CDM and **`>= 0.70` gives CAM**. In a five-row grid an attacking midfielder at
  **row 4 with defRow 1 and fwdRow 5 computes (4-1)/(5-1) = 0.75**, which clears the bar , but at
  **defRow 2 and fwdRow 5 it computes (4-2)/(5-2) = 0.667**, which does not, and falls through to
  CM. That single case would produce exactly the observed shape: CM absorbing what CAM lost while
  CDM barely moves.
  **IT CANNOT BE TESTED FROM THE DATABASE, WHICH IS THE WHOLE BLOCKER.** The importer upserts only
  `api_player_id, season_year, league_code, position, appearances, distribution, shirt_number` ,
  **`row`, `col`, `rowWidth`, `defRow` and `fwdRow` are never persisted**, so the grid that decided
  each call is gone. Persist them first, then re-pull.
- **Merge to `vvonderxi_BIGGER`** , blocked on the Step 5 hygiene items and the QA pass.
- **Compare picker honours filter** , not blocked any more, see stale item 3.

---

## 5. DOCUMENT MAP

| read | for |
|---|---|
| `CLAUDE.md` | **source of truth.** §C invariants, §D plan, §E data threads, §F session log |
| this file | orientation, then throw it away when it goes stale |
| `docs/KEEPER_FALLBACK_C_SPEC.md` | the keeper contract. Authored in the Fable session, verbatim |
| `docs/CABINET_SPEC.md` | the Cabinet ruling, verbatim. `_NOTES.md` holds measurements and departures |
| `docs/FABLE_PAYLOAD_BRIEF.md` | what the prose model is told. `_NOTES.md` holds the audit |
| `QA_PASS.md` | 42 items, four groups. The merge gate |
| `RULE_EVIDENCE.md` `SILENT_FAILURES.md` `DATA_DEFECTS.md` | the evidence behind §C and §E headlines |
| `POST_LAUNCH.md` `LAUNCH_STAGE.md` | deferred specs, read when that stage starts |
| `INGESTION_RECOVERY.md` | read before any re-ingest |
| `scripts/separability/README.md` | the engine re-transcription, 99.56% exact |
| `docs/TOP_ASSISTS_SOURCING_SPEC.md` | **read SS 0 and SS 9 before any external sourcing job.** Card resolution is a precondition; the confirmation bar is lineage, not source count |
| `migrations/positions_2526_2026-09-11/README.md` | the position batch, and the measured verdict on `pool_change_sim.js` |

**SUPERSEDED, do not work from:** `docs/HANDOVER_2026-09-04.md`, `HANDOVER_2026-08-20.md`,
`HANDOVER_2026-08-09.md`. `CLAUDE_ARCHIVE.md` and `CLAUDE_ARCHIVE_2026-07.md` are frozen history
, open only for evidence behind something already recorded as settled.

---

## 6. HOW LUCAS WORKS

- **Two terminals, strictly. Terminal A is you** , edits, SQL, commits. **You never push.**
  **Terminal C is his plain shell** , reads, verification, pushes. He pushes, always. Label
  every command with its terminal.
- **Claude in Chrome** for browser work and dashboards. Render and measure rather than reason
  about layout: §C records that reading markup missed defects a real layout exposed at once.
- **Demo before build on anything visual.** Show two or three options, say which you would ship
  and why. Pick once, build once.
- **One pasteable block at a time**, labelled with where it goes. He is a solo non-coder
  founder: "here is the SQL, paste it in Supabase" beats "run a query to check X".
- **Decisions front-loaded, then the reasoning.** Concise. Numbered choices when picking.
- **No em or en dashes anywhere. Spaced comma.** `NR` for missing data, never 0.
- **Voice-to-text input** , read for intent, not literal transcription. Typos and run-ons are
  normal. **He often cites figures from memory that do not reproduce; check them against the
  repo or the database before acting, and say plainly when they do not match.**
- **Own mistakes plainly, no grovelling.** Flag the better option; do not hedge.
