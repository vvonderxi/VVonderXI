# HANDOVER , 2026-09-04

**Entry point for a new planning chat.** Read this, then `CLAUDE.md` in full. `CLAUDE.md`
wins on any conflict; this file is routing, not a second source of truth.

**Every figure here was measured against the repo or the database on 2026-09-04.** Where a
claim came from an earlier document and the measurement disagreed, the measurement is
recorded and the stale claim is named. Re-measure before acting , this file will go stale
the same way the last two did.

---

## 1. WHERE THE PRODUCT IS

### What is live on vvonderxi.com right now

**[SUPERSEDED THE SAME DAY , READ THIS FIRST.] VERCEL'S PRODUCTION BRANCH WAS CHANGED TO
`coming-soon` ON 2026-09-04**, in Settings > Environments > Production, and a deployment was
promoted at **`0515e00`**. **`vvonderxi_BIGGER` now builds as a PREVIEW and deploys nothing.**
The live page now carries the newer copy ("No noise until kick-off.") and the favicons, and
`favicon.svg` returns 200. **Everything in this subsection below describes the state BEFORE
that change** and is kept because it records how the diagnosis was reached.
**IT FLIPS BACK WHEN THE PLATFORM SHIPS** , the merge target is still `vvonderxi_BIGGER`, so
the setting must be reset or the merged platform deploys nowhere.

**A holding page, served from `vvonderxi_BIGGER` at `afb9970`.** Proven by byte-comparison:
`git show vvonderxi_BIGGER:index.html` is md5-identical to the live page, 30,005 bytes both
sides. `coming-soon` is 30,892 bytes and does not match.

    last-modified   Wed, 02 Sep 2026 16:12:18 GMT
    title           VVonderXI , Every Season Tells a Different Story
    og-image.png    200
    favicon.svg     404
    /card           404

`vercel.json` on that branch rewrites `/(.*)` to `/index.html`, so every path serves the
holding page. **Zero serverless functions are deployed** (`git ls-tree -r vvonderxi_BIGGER
-- api/` is empty), which is why the Hobby 12-function cap no longer binds.

**THE FULL SITE IS NOT LIVE. There is no card, compare, rankings or playbook in production.**

### The trap that has already cost time here

**`coming-soon` is not the deployed branch, and two commits on it are invisible.** The copy
change `c0b8107` ("No noise until kick-off.") and the favicon commits `bdbe049` / `c3b30f3`
are pushed to `origin/coming-soon` and are **not on vvonderxi.com**. The live page still
reads *"One message when it opens. Nothing else, ever."* and `favicon.svg` 404s.

This was first diagnosed as a stale deploy. **It is not.** The deploy is current for
`vvonderxi_BIGGER`; the work is on a branch Vercel does not build. **Before editing the
holding page, confirm which branch Vercel's production branch setting points at.**

### What `redesign-compare` holds that production does not

    git rev-list --left-right --count vvonderxi_BIGGER...redesign-compare
    10      19
    git diff --shortstat vvonderxi_BIGGER redesign-compare
    214 files changed, 213197 insertions(+), 434 deletions(-)

Merge base is **`775095f`**. `redesign-compare` (216 files) carries the entire platform:
card, compare, rankings, playbook, vvindex, `vv-core.js`, `vv-marks.js`, the tag engine, the
verdict layer, the goalkeeper card, the share posters, and 2 API functions (`analyse.js`,
`get-seasons.js`).

**THE MERGE IS NO LONGER A FAST-FORWARD, AND `QA_PASS.md` STILL SAYS IT IS.** That file
records merge-base `5bdbadb`, production "0 ahead", "clean fast-forward, zero conflicts",
and 639 commits. **All four are stale.** Production is **10 commits ahead** of the merge
base because it was repurposed into the holding page. `QA_PASS.md` C10 asserts "0 ahead" as
a pass condition and will fail. **The merge shape has to be re-decided before the QA pass is
run.**

### The branches

| branch | tip | files | what it is for |
|---|---|---|---|
| `vvonderxi_BIGGER` | `afb9970` | 5 | the holding page. **WAS production; now builds as a PREVIEW since 2026-09-04.** Still the MERGE TARGET. |
| `redesign-compare` | `fcc0555` | 216 | **the platform.** All development. Current working branch. |
| `coming-soon` | `0515e00` | 9 | **production since 2026-09-04.** Serves vvonderxi.com. Carries the newer copy, the favicons and the mobile form fixes. |
| `vvonderxi_ORIGINAL` | `5bdbadb` | 33 | the pre-holding-page production site, preserved. This is the old merge base. |

All four are level with their `origin/` counterparts (0 ahead, 0 behind) as of 2026-09-04.

---

## 2. WHAT JUST HAPPENED

### No repair has been run. The database is untouched.

**`player_season_cards` = 57,055 rows. `player_card_mv` = 57,055. 53,994 carry an rt.**

A full-table diff against an in-session snapshot of all 57,055 cards returned **0 cards with
a changed rt, 0 with changed minutes, 0 added, 0 removed.** No write has been made to any
table.

**`sumStat` was not wired, because it is already wired**, at `scripts/import/import-players.js:506`,
and it is the live path. Verified three ways: the call site; 36 of 36 multi-block seasons in
PL 2023 + SA 2023 summing arithmetically correctly today; and `DATA_DEFECTS.md` recording the
same thing ("the resolver is not the cause ... the stored rows were written by an EARLIER
version of the summing path"). **There is no code change to make there.**

### What was actually done: a full read-only pre-flight scan

**144 of 144 league-seasons, 5,121 provider calls, zero fetch failures**, using the real
`resolveSeasonStat` against live API-Football responses. Output is per-league-season JSON in
the session scratchpad and is not committed. It found three populations:

| population | measured | what the plan said |
|---|---|---|
| **P1 halved** (resolver sums, stored row holds one block) | **1,462** (968 material) | 549 |
| **P2 dedupe-mismatch** (a re-import relabels the club) | **261** | not in the plan |
| **P3 split with no stored card** | **298**, of which **28 importable** | 2,308 |

**Both gating figures mismatched, so the run was stopped before any write**, which is what
the instruction required.

**P2 is the finding that changes the repair.** The provider now returns a duplicate block on
past seasons carrying the player's **current** club. Verified 6 of 6 against the provider's
own 2025/26 data: Matheus Cunha (Wolves + Man Utd), Aït-Nouri (Wolves + Man City), Elanga
(Forest + Newcastle), Garnacho (Man Utd + Chelsea), Lindelöf, Osula. **Our stored club is
correct in every case and a default-mode re-ingest would overwrite it.** Concentrated
2022-2024; near zero before 2021.

**Two claims in `migrations/transfer_halves_scale/PLAN.md` are now falsified:**
- *"every repair RAISES a score"* , of 968 material repairs, **461 raise the per-90 rate and
  287 lower it.** `INGESTION_RECOVERY.md` already predicted this for Rashford 2024
  (rt 82 -> ~72).
- The ~1,600 estimate , the real figure is **1,462**, close, but 549 is not.

**Assertion 3 of that plan (`_shape:'summed'` only) is doing more work than it was written
for.** It structurally excludes all 261 P2 cards and every revision-drift card. **Do not
relax it.**

### A separate population found by sampling, not in the scan

**Roughly 5% of all cards disagree with the provider's current figures**, median 7 minutes,
direction exactly symmetric (35 up / 35 down in a 70-card pooled sample). Not a different
season, not a cup, not an adjacent year , the same league block, revised since import.
**17% of them are a material loss if re-imported**, worst measured: Romário Baldé PRT 2015,
ours 1,405m/24ap/2g against the provider's current 708m/10ap/0g. A further ~1.15% of cards
are ones the provider **no longer serves at all** (José Fonte PL 2013, 3,181m), where our
row is the only surviving copy.

**This is the case against a default-mode re-ingest**, and it does not touch the scoped
repair, because 96% of those cards resolve to `_shape:'single'`.

### Career tags were not recomputed, because nothing moved

Baseline measured 2026-09-04 by running the shipped `VVCore.careerStageTags` over all 57,055
cards: **329 Peak, 152 Breakout, 413 The Standard** (card counts; `f9a93b8` records 310 /
132 / 58 as player counts). **38 Peaks are tied at zero margin, 152 within 3 points.**

**The exposure if the repair runs is small and was mis-stated once before correction:**
- **0** material halved cards currently hold Peak
- **2** players hold a Peak elsewhere and have a material halved season within 10 points of
  it (A. Delort L1 2021, G. Laborde L1 2022)
- **0** cards positioned to create a new Peak by crossing 85
- **1** Breakout exposure (A. Semenyo PL 2025), **7** for The Standard

An earlier figure of "170 within 5 points" counted every material halved card regardless of
whether Peak was reachable at all. Peak needs rt >= 85; most halved cards sit at rt 74 median.

### `vv-core.js` is modified and uncommitted  ,  **[LANDED 2026-09-04, SEE THE BLOCK BELOW]**

One change: the `career.length < 4` guard in `careerStageTags` relaxed to `< 1`, plus a
comment. Measured effect: **Peak 311 -> 329, Breakout 134 -> 152, The Standard unchanged at
413.** The comment claims 19 players; 18 are reachable because `card.html:1721` returns early
below 2 seasons.

**It should be committed.** It belongs with `f9a93b8`. **Sequencing: it moves the tag
baseline, so any repair's "before" snapshot must be taken after this commit**, or the repair
gets blamed for +18/+18 that are this one line.

**[DONE 2026-09-04. THIS HANDOVER PREDATES THREE COMMITS ON `redesign-compare`.]**
- **`cdeabb3` fix(tags): the career guard was an analysis artefact, not a rule.** The change
  above, committed. **Tag baseline moved 311 -> 329 Peak and 134 -> 152 Breakout**;
  The Standard unchanged at 413. Any before/after snapshot must be taken AFTER this.
- **`f8981da` fix(filters): three Career-Stage chips could never match anything.** Peak,
  Breakout and The Standard are marked `soon:true` per item and render disabled with a
  "soon" marker, because `clientPredicate` reads `card.tags` and **`getVVTags` never calls
  `careerStageTags`**, so they returned silent empties. Per-item rather than `where:'inert'`
  on the group, which would have disabled Wonderkid and The Last Dance too.
- **`10b85e4` fix(tags): Wonderkid and The Last Dance join the career-stage family.** `AGE`
  merged into `STAGE`; **all five career-stage tags now render teal** `#2F8290 -> #1B5563` on
  face, rows, season rows and glance. **`STAGE:0` in `PRIO` is load-bearing** , without it
  `prio()` falls back to 1 and Wonderkid drops off the card face on a max-2 render, silently,
  on 114 cards. Verified over 121,935 rendered tag lists: **0 order differences**, 1,710 class
  differences. The `-age` rules are left in place and marked dead.

**A latent defect found while measuring, not caused by this change:** the career-stage rules
match on `season_year` rather than `card_id`, so a player holding two cards in one
`season_year` (Bruno Fernandes 2019, PRT and PL) can receive the tag on both.
**[SIZED 2026-09-04 , THE MECHANISM IS REAL AND THE EXPOSURE IS TWO CARDS.]** The earlier
wording read as a class of problem. Measured over all 57,055 cards: **571 players hold two or
more cards in a single `season_year`, and exactly ONE receives a stage tag on both**,
affecting **2 cards**. Nine players carry a stage tag on more than one card in total, but
eight of those are The Standard behaving as designed , it is carried by every qualifying
season. **Worth fixing on principle, not worth treating as a live defect.**

### Career-stage tags render in the wrong container

**`f9a93b8` recorded Peak, Breakout and The Standard as shipped. The rules are; the rendering
is not.** They compute correctly on **329 / 152 / 413** cards, and they appear on exactly one
surface: `card.html`'s **Wonder Tags** panel. Not the card face, not the Glance, not rankings
rows, and not filterable.

**And that one surface puts them in someone else's row.** `card.html:1705` does
`document.querySelector('.tagrow')`, which returns the FIRST tag row on the page , Palmer
23/24 has six. Measured: the tag lands inside **Iconic** on Palmer, **Ballon d'Or** on Messi
11/12 and **Golden Boot** on Ronaldo 14/15, reading as part of that honour's description.

**`VVCore.vvMarkFor` does not exist**, so the mark falls to an empty string and never draws;
the ternary guard means nothing throws. The element is a bare `<span class="tt">`, and `.tt`
is the tag TITLE class inside a Wonder Tags row, so it inherits heading styling rather than
pill styling. **`vv-marks.js` has no symbol for any of the three.**

**Do not read `f9a93b8` as "these are on the card".** Full record in `CLAUDE.md` §D.

### The null-pool position batches

**They are generated and on disk. They have NOT been sent to Fable.**

    research/nullpool/batch_1.csv    41 rows + header
    research/nullpool/batch_2.csv    41 rows + header
    research/nullpool/batch_3.csv    40 rows + header

**122 rows total**, 14 columns, header byte-identical to `cam_tail_batches/batch_01.csv` so
`cam_tail_batches/PROMPT.md` applies unchanged. `verified_position`, `confidence` and
`evidence` are empty for Fable to fill.

Population: `player_card_mv WHERE position_pool IS NULL AND rt >= 80`. **122, confirmed by a
paginated select and an independent exact count.** A figure of 405 or 411 was in circulation;
**it corresponds to no threshold on this database** , rt >= 76 gives 366, rt >= 75 gives 861,
rt >= 80 gives 122.

**44 of the 122 were already tested on 2026-08-21 and held** (43 medium, 2 low, 2 UNSURE),
recorded in `research/nullpool_verified/HELD_2026-08-21.md`. **Every card above rt 82 is in
that held group**; the 78 never-asked all sit at rt 80-82.

**The 2026-08-21 batch is worth reading before running this one**: commit `0df2721`, 82 cards
tested, **37 written high-confidence, 45 held**. All 37 landed , verified 2026-09-04, 37 of 37
now carry a `position_pool` in the matview. An earlier statement in this session that the
Option E backfill had never run was **wrong**.

**When the batches return:** write only `high` confidence, leave `UNSURE` unwritten, INSERT
into `player_positions` with `ON CONFLICT DO NOTHING`, record the held rows in a `HELD_` file
beside the returned CSV, and refresh the matview. The 2026-08-21 commit message is the
template , it asserted 6 counts before and after and predicted read-only correctly, because
`pool_ingr` is gated on `season_year >= 2016` and those cards were older.

---

## 3. WHAT IS PENDING

### Needs Lucas , decisions and account actions

| item | state on 2026-09-04 | blocked on |
|---|---|---|
| **Which branch Vercel deploys** | production = `vvonderxi_BIGGER`; `coming-soon` has newer copy and favicons and is not built | a look at the Vercel project settings |
| **API-Football cancellation** | plan **Ultra, active, expires 2026-09-17T16:04Z**, 12,561/75,000 used today | a decision before renewal. The scan cost 5,121 calls; nothing outstanding needs Ultra |
| **Vercel Pro** | recorded as ON in `CLAUDE.md` §C, not re-verified here | production now deploys **0 functions**, so the Hobby 12-cap no longer binds. Pro is a commercial-use requirement, not a technical one, while the holding page is all that is live |
| **Revoking BSD at the provider** | `BSD_API_KEY` deleted from Vercel and GitHub secrets. **Revocation at the provider is NOT confirmed.** 6 files still mention it, all records or comments, no credentials | one action in the BSD dashboard |
| **The "model comparison"** | **not found in the repo.** No file or doc uses that phrase. Nearest is the parked Sportmonks provider evaluation in `POST_LAUNCH.md` and the untracked `sportmonks_killtest_report.json` (generated 2026-07-19) | Lucas naming what it refers to |
| **Q5, may a card name two clubs** | ruled: destination club as `team_id`, both clubs in the label. Not built | see the dependency below |
| **The transfer-halves repair** | scoped, pre-flight run, **not executed** | a go, given the real 1,462 / 261 / 28 figures |
| **Lowering `MIN_MIN`** | 300-minute floor holds absolutely; no card below 300 exists | a platform decision. The provider offers ~49,000 sub-floor cards; adding them shifts `rel_pct` **+24 points at the median** and re-scores the whole database |
| **`api/log.js`, waitlist data** | `waitlist_emails` holds **3 rows** | a privacy decision, recorded in `CLAUDE.md` §E |

### Needs the assistant , buildable once unblocked

- **The transfer-halves repair itself.** Scoped, assertions written (§3 of the previous
  session's output and `migrations/transfer_halves_scale/PLAN.md`, with the direction claim
  corrected). Per-card lists for all three populations exist but are in the session
  scratchpad and **are not committed** , they must be regenerated or the scan re-run.
- **Import the 28.** Insert-only, additive. **12 of the 28 are the documented Estrela
  PRT 2025 blank-team deferral healing itself** now that the provider has backfilled the club
  name; `INGESTION_RECOVERY.md` calls it self-healing by construction. The other 16 are older
  misses (Bebé LL 2018, Bednarek PL 2022, Bolasie PL 2016, Nanu PRT 2020).
- **Q5 implementation.** The destination is **not derivable from the stats response**:
  first-block is right 35/36, richest-by-minutes 26/36, **`/transfers` 36/36**. Needs one
  extra call per summed card. **And `teamHonoursFor()` in `vv-core.js` keys on an exact
  `honTeamNorm(card.team_name)` match**, so a two-club string in `team_name` silently deletes
  League Champion and UCL Winner. The clean shape puts the destination alone in `team_name`
  and the second club in a **new column**, which costs a matview DROP + CREATE.
- **The shared matview rebuild.** Four deferred items need one: the known-as name fold, the
  Proof percentile columns, the honour flags, and now the second-club column. **One sitting or
  none.**
- **Peak, Breakout, The Standard** , recomputed after the repair and after the Fable writes,
  diffed by `card_id` and not by count.
- **The Step-6 QA pass** , `QA_PASS.md`, 42 items. **Its merge-shape figures are stale** (see
  §1). Group A is largely passing; A14 contrast is the big open one.

---

## 4. HOW WE WORK

- **Two terminals, strictly.** Terminal A (this assistant) does edits, SQL and commits and
  **never pushes**. Terminal C (plain) does reads, verification and pushes. **Lucas pushes,
  always.**
- **Commits wait for an explicit go.** A descriptive line from Lucas is a commit message, not
  approval to commit.
- **Demo before build.** On any multi-option design choice, show it first. Pick once, build
  once. Premium and motion polish is a dedicated pass **after** the core build, never
  interleaved.
- **Large pastes drop in the planning chat.** Prefer a file on disk plus a short confirmation
  over a long paste. **Prove a file exists with `ls` in the same message that claims it** ,
  paths have been reported that the other side could not open, three times in one session.
- **Read for intent.** Voice-to-text input; typos and run-ons are normal.
- **Say what is incomplete.** Distinguish "verified live this session" from "recorded in a
  doc". Do not soften a measurement to match a plan.
- **No em or en dashes anywhere.** Spaced comma. **NR for missing data, never 0.**
- **Never `git add .`** , stage named files. **Never `git reset --hard`** , use
  `git checkout -- <named files>`. There are 39 untracked scratch files in the tree that must
  stay untracked.
- **Verify against live code and the database before editing.** Docs drift, including this
  one. Three figures acted on in this session (549, 2,308, 405) did not survive measurement.

---

## 5. WHICH DOCUMENTS MATTER

**Read in this order.**

| file | what it is for |
|---|---|
| **`CLAUDE.md`** | **The source of truth.** Locked invariants (§C), the launch plan (§D), open data threads (§E), the session log (§F). Wins on any conflict. ~143 KB. |
| `docs/HANDOVER_2026-09-04.md` | this file. Routing only. **Replace it rather than patching it, and update the pointer in `CLAUDE.md` in the same commit.** |
| `QA_PASS.md` | the 42-item Step-6 gate, the only thing standing between the branch and production. **Merge-shape figures are stale.** |

**Evidence files , open when something has already gone wrong:**

| file | what it holds |
|---|---|
| `SILENT_FAILURES.md` | post-mortems on changes that did nothing and looked fine |
| `RULE_EVIDENCE.md` | the measurements behind the §C rules |
| `DATA_DEFECTS.md` | forensics on the known data defects |
| `INGESTION_RECOVERY.md` | the closed ingestion-gap recovery; read before any re-ingest |
| `DOC_MAINTENANCE.md` | how and when `CLAUDE.md` gets relieved |

**Stage files , open only when that stage starts:**

`POST_LAUNCH.md`, `LAUNCH_STAGE.md`, `ACCOUNTS_STAGE_SPEC.md`, `DB_HYGIENE_STAGE.md`,
`VVonderXI_Engine_Design_Log.md`, `docs/FABLE_DECISIONS.md` (+ `FABLE_BRIEF.md`,
`KEEPER_SCORING_BRIEF.md`), `migrations/transfer_halves_scale/PLAN.md`.

**Superseded , do not work from these:** `HANDOVER_2026-08-09.md`, `HANDOVER_2026-08-20.md`.
`CLAUDE_ARCHIVE.md` and `CLAUDE_ARCHIVE_2026-07.md` are frozen history.

---

## THE SHORT VERSION

**A holding page is live from `vvonderxi_BIGGER`. The platform is 19 commits of divergence
away on `redesign-compare` and the merge is no longer a fast-forward. The database has not
been written to. The transfer-halves repair is scoped and stopped at the gate because the
pre-flight measured 1,462 / 261 / 28 against a plan written for 549 / 2,308. Three position
batches are on disk and not yet sent. One line of `vv-core.js` is uncommitted and should go
in.**
