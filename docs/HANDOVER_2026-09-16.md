# HANDOVER , 2026-09-16

## READ THIS FIRST. NONE OF IT IS LIVE.

**Everything below is on `redesign-compare`. Vercel's production branch is `coming-soon`, which
serves the holding page. `vvonderxi.com` is running none of this.**

That matters more than usual today, because **35 commits landed in two days and the repository
now reads as a shipped platform.** The only deployed function, `api/analyse.js`, gained an
input/output bound, an origin allowlist and a rate limit , **and a reader of the code would
conclude the endpoint is protected. It is not yet.** The protections become real at the
production-branch flip, which is the launch, not the merge.

**Scope against the current merge-base `32b19dab`: 136 files, +37,457 / -1,046.** Not the
599-commit figure in §D , that was measured against a base the 2026-09-06 merge superseded.

---

## WHAT SHIPPED

**Security and the API surface.** The deployed surface went **2 functions to 1**: `api/log.js`
was already gone and `api/get-seasons.js` was deleted , it had **no caller in any `.html` in the
entire history of any branch**, and was asserting a recorded `0` for assists on 31,040 rows.
`api/analyse.js` now bounds output (2048 tokens), input (120k chars) and message shape, refuses
an absent or unlisted `Origin`, and rate-limits 30 new generations/hour/IP with 2 concurrent.
Details and the spend figures: **`docs/MERGE_READINESS_2026-09-15.md`**.

**Caches.** The verdict stamp is fingerprinted from the prompt actually sent rather than a flag;
`VERDICT_VERSION_JUDGE` is exported; a row that cannot be stamped is no longer written. 194
orphan notes rows deleted and `comparison_log` / `search_log` dropped, all captured first , see
`migrations/notes_orphans_2026-09-15/` and `migrations/orphan_endpoints_2026-09-15/`.

**Compare and card.** The seventh tag leak (a finished tag surviving into the next pairing), the
season fold missing entirely on deep-linked pairs, and the stepper's 8.7% horizontal stretch ,
which was `max-width` changing at the transition boundaries, not an easing problem.

**Honours.** The filter taxonomy and chip labels now derive from `HONOUR_META`. The two
continental types render as inert `soon` chips **because they have no matview column** , that is
the real gap, and it rides the sitting.

**Product.** Item 7's carry-over offer on the card search (named, never assumed) and item 9's
starter pills on the home page (always visible, handing off to rankings). The Playbook's two
story blocks no longer fold. The PDF gained its cover mark.

**Five undefined CSS variables**, all live, including rankings' "Filters" button rendering with
no background and no border.

---

## WAITING ON LUCAS , WHERE TO LOOK

| | where |
|---|---|
| **Spend cap and alerts** , the only instrument that sees distributed abuse. $100 cap, alerts at $20 and $50 | `QA_PASS.md` **B4b**, derivation in the merge-readiness note §4 |
| **The matview sitting** , 87 columns, three passengers, SQL editor's lane | `migrations/matview_sitting_2026-09-15/` |
| **The PDF cover mark** , judged on a printed page, opened through the server | `docs/pdf/vv-index.html` |
| **The VV Index rebuild** , still an **untracked demo**, `_demo_vvindex.html`; the live page has neither corrected figure | item 10 |
| **The stepper and Playbook demos** | `_demo_three.html` |
| **The blank-shield question** , governs two populations, answer both together | §D, and `docs/FOR_LUCAS_transfers.md` |
| **Item 25's measurement** , gated, and the gate is explicit | below |

**Item 25 is withdrawn, not pending.** Every verdict marker figure recorded before 2026-09-15 is
void , the n=11 set spans **five prompt bases including Path B**. The notes figures survive that
but fall on a moving baseline. **The gate is not "rows exist", it is "both prompt bases have
stopped moving", and the current Path A prompt has ONE row.** The correct filter is written out
in §C: split `cache_version` on `-`, keep the first two segments, compare against the three
exported constants, report Path A and B separately.

---

## THE ONLY QUEUED JOB

**The platform contrast re-run, QA A14, as its own session.** The buttons-and-chips half of the
2026-09-01 survey is void , the instrument grounded self-painting elements against their parent,
which under-reports and can pass an element that fails. **Do not read their absence from those
results as a pass.** Large enough to warrant a session; nothing else is blocked behind it.

---

## FOUR THINGS THAT WILL SAVE THE NEXT SESSION TIME

1. **A behaviour attached to a code path rather than to the state it depends on is missing from
   every other path, silently.** Four instances now , the wait class, the tag reset, the season
   fold, and the filter stash. The fourth was caught by the rule before the bug existed.
2. **Before quoting any cached-population figure, ask what fraction of the denominator is
   ELIGIBLE to be counted**, not merely what has arrived. Three wrong numbers in one day came
   from this.
3. **A derived list over an incomplete schema is worse than a hand list** , it turns a missing
   feature into a broken query.
4. **`grep -c` counts lines, `pkill -f` matches the shell running it, and a page's HTML is not
   cache-busted.** All three cost time in the last two days.
