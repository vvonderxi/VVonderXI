# VVonderXI , HANDOVER, 2026-08-09

**THIS SUPERSEDES EVERY EARLIER HANDOVER DOC. There are 24 of them, the newest dated 2026-07-05, and all of them predate this session entirely. If you are reading one of those, stop and read this instead.**

Read this file FIRST, before `CLAUDE.md`. `CLAUDE.md` is 122 KB and is the full source of truth; this is the entry point so you do not have to read it to find out where the project is. Once you know where you are, `CLAUDE.md` §F (session log, newest at top) has the detail, and **`CLAUDE.md` wins on any conflict with this file.**

---

## 0. VERIFY YOU ARE WHERE THIS FILE THINKS YOU ARE

Branch: **`redesign-compare`**. Production is `vvonderxi_BIGGER` and is NEVER touched directly.

**First command of the session, in Terminal C:**

```
git log --oneline -4
```

Confirm the top commits are **position or tag work dated 2026-08-09 or later**. They should look like this, most recent first:

```
data(positions): risk report , 798 unchecked cards, and the CM/CDM blind spot
docs: section F , position programme run completed, rule hit rate 21%
data(positions): clearing rule chunk 5 (final) , 4 of 12 qualified, 7 rows
data(positions): clearing rule chunk 4 , 3 of 20 players qualified, 3 rows
```

**If the newest commit predates 2026-08-09, you are on a stale tree , STOP and ask.** Everything below assumes this session's work is present.

**If there is work NEWER than 2026-08-09, this file is behind.** It is still a valid orientation, but read `CLAUDE.md` §F (newest entry at top) for anything that happened after, and treat §F as authoritative where the two differ.

**No commit hash is named here on purpose.** Earlier versions of this file pinned an exact hash. That breaks twice over: a commit cannot contain its own hash (writing it changes the hash, forever), and any commit landing on top makes the pin wrong even when nothing is actually amiss. A guard that cries wolf on healthy progress trains the next session to ignore it, which is worse than no guard. **Do not "improve" this back into a hash.**

Nine commits were unpushed when this was written. Lucas pushes, always , never push for him.

---

## 1. WHAT THIS PROJECT IS, IN FOUR LINES

A football ratings site. Every player-season is a card with a **VV score (`rt`, 11 to 97)** produced by an engine that lives in a Postgres view. 57,234 cards across 9 leagues, 2010-2025. The site is static HTML reading a Supabase materialized view (`player_card_mv`) directly from the browser. Lucas is a solo, non-coding founder: give him decisions and the literal command to paste, labelled with which terminal.

---

## 2. POSITION WORK , the thread that dominated this session

Every card sits in one of **8 position buckets**: GK, FB, CB, CDM, CM, CAM, Winger, ST. A systematic upstream fault was dumping wingers into **CAM**, so the bucket was polluted.

**208 cards reclassified, across fourteen writes, all rt-verified before and after.**

**CAM pool 1,870 -> 1,685. ONE public band crossing in the entire programme** (Soucek 20/21, rt 79 -> 82, Accomplished -> Standout). Batches:

| batch | cards | source |
|---|---|---|
| Tier A | 24 | Transfermarkt, hand-verified |
| pass 2 | 31 | two independent research passes agreeing |
| tail 01 | 35 | Fable research, batch 01 |
| tail 02-05 | 40 | Fable research, batches 02-05 |
| Coman | 7 | Transfermarkt, rule-qualified (see below) |
| Nico Williams | 2 | manual override, source `knowledge` |
| clearing rule, 5 chunks | 47 | Transfermarkt profiles, 92 players read |
| pass-1 residue | 1 | Richarlison 17/18 |

**THE RULE THAT CLEARED COMAN, and it is narrower than it looks.** A held row can be cleared without per-season research ONLY when the player's Transfermarkt **main position AND every listed other position are wide**. Coman is main Left Winger, other Right Winger, no central or striker role anywhere. **The broader "his other seasons were Winger, so this one is too" heuristic was TESTED AND FAILED**: of 11 players checked, 10 had a wide main position, but Pépé's other positions are Centre-Forward and Second Striker (so a held season of his might be ST), and Coutinho is main **Attacking Midfield** with wide secondaries. Bulk-clearing on same-player would have written plausible, untraceable errors. **FINAL HIT RATE: 21 of 100 players, 47 of 226 rows , 21%.** Chunk by chunk: **0/20, 5/15, 5/15, 3/17, 4/8**, plus the first 4/8 pass.

**WHY CHUNK 1 RETURNED ZERO, and this is the part to internalise: the rule fails on HIGH-rt players because they are genuinely multi-role.** Chunk 1 was the most visible eligible players (rt 83 down to 74) and Sarabia, Bernardo Silva, Szoboszlai, Fornals and Shaqiri all read central-main on Transfermarkt. **The rule's yield RISES as rt falls** , the opposite direction to the research-confidence curve, which falls as rt falls. A future session seeing a barren top chunk should NOT read it as the rule breaking.

**79 players were excluded as genuinely multi-role.** Commonest secondaries: Attacking Midfield, Centre-Forward, Second Striker. **Right-Back also appears** (Corona, Ndao, Sahiner, Diatta), so the rule correctly excludes a DEFENSIVE alternative too, not just a central attacking one. Those 79 need per-season work; no rule shortcuts them.

**THE RULE THAT EXPLAINS IT, and the next batch will otherwise be surprised by it: CDM is the only destination pool where the defensive signal is load-bearing.** All four cards that moved rt at all were `CAM -> CDM`. Every `CAM -> Winger` and `CAM -> CM` row moved exactly zero, because for attackers the bounded `def_core` loses to the attacking path in the best-of. **Expect rt movement only on CDM transitions, and expect it upward.**

### THE BIGGEST OPEN RISK , 798 cards that entered no sweep at all

**981 cards at rt>=70 have a coarse MID/FWD position and sit in CAM/CM/CDM. 798 of them have NEVER been written or held.** And **731 of those 798 are in CM or CDM, which every CAM-scoped pass was structurally blind to** , the Nico Williams 25/26 case generalised: she was missed not because research failed but because the sweep could not see her bucket.

The top of the list is not obscure: **De Bruyne 19/20 rt91, Bruno Fernandes 20/21 rt90, Yaya Toure 13/14 rt90 (CM), Eriksen 16/17 rt89 (CDM), Alli 16/17 rt89, Palmer 24/25 rt88.**

**SAY THIS PLAINLY SO NOBODY OVERREACTS: most are probably CORRECT.** De Bruyne, Eriksen and Ozil genuinely are central players. **The risk is the ABSENCE OF CHECKING, not the presence of error** , but the coarse classifier demonstrably produces misfiles like Eriksen at CDM and Yaya Toure at CM, so the population cannot be assumed clean either.

Full ranking in `position_risk_report.txt` (4 groups). The launch-relevant subset is `position_bigclub_review.csv` , 236 big-club cards, 184 unchecked, with **Arsenal 18/18, Inter 12/12 and Juventus 10/10 entirely unchecked.**

**RECOMMENDED SHAPE: treat the big-club list as the launch list, and the long tail as a DISCLOSED LIMIT rather than a work queue.** 798 cards at the measured research yield is not a pre-launch project.

### IN FLIGHT RIGHT NOW

**`bigclub_batches/` 01-05 , 184 cards, 99 players, running through Fable.** Same contract as the CAM tail: reuse `cam_tail_batches/PROMPT.md` unchanged, with one caveat , `current_pool` here is CAM, CM **or** CDM, so the research must not assume the starting bucket. Expect a HIGH confirm rate (these are genuinely central players) and a HIGH confidence curve (best-documented players in the dataset). If the curve collapses the way the tail did at rt 56, something is wrong with the RUN, not the players.

**After that: `cam_tail_batches/` 06-21** , but read the next section first, because the measurement argues against running them.

### Batches 06-21 were deliberately NOT run

The tail was split into 21 batches ordered by rt descending. Only 01-05 ran. **This is a decision backed by measurement, not an oversight.**

The research reports a confidence level per row, and only `high` rows are written. That rate collapsed as the players got less prominent:

```
batch 01  avg rt 78.1   65% high      batch 04  avg rt 60.3   3% high
batch 02  avg rt 64.0   15% high      batch 05  avg rt 56.6   3% high
batch 03  avg rt 64.4   13% high
```

**At 3%, running the remaining ~1,150 cards would put ~97% of them straight into a hold file.** Below roughly rt56 the players are non-famous and in smaller leagues, and reliable per-season role information genuinely runs out. Either accept the tail stays CAM-labelled and disclose it, or find a source that actually covers small-league players. **Do not just run it because it is there.**

**A falling curve is the healthy signal. A FLAT curve is a WARNING** , it means the research stopped saying UNSURE and started guessing with a confident label. The writer warns automatically on two consecutive non-falling batches.

**If the tail is ever revisited:** the prompt is `cam_tail_batches/PROMPT.md` (tracked, load-bearing , it carries the UNSURE emphasis and the identity contract). Batches 01-05 are tracked; 06-21 are gitignored and regenerate with `NODE_PATH=./node_modules node scripts/enrichment/make_cam_tail_batches.js`.

### What is waiting, unresolved

| file | rows | what it is |
|---|---|---|
| `scripts/enrichment/tail_HOLD.csv` | **297** | **ACTIONABLE.** A different position was proposed but at medium/low confidence, or UNSURE. Nobody has settled these. Regenerated after the Coman and Williams writes, so it contains no resolved rows. |
| `scripts/enrichment/tail_LOWCONF_AGREE.csv` | 162 | **NOT actionable.** Research agreed with the current bucket, just weakly. Nothing to do. Kept so a later pass can tell "nobody looked" from "someone looked and agreed". |
| `scripts/enrichment/pass2_HOLD.csv` | 27 | Held from the earlier pass-2 batch: 3 genuine disagreements, partials, and live 25/26 seasons whose second half could not be verified. |

**The 85 pass-1 rows are CLOSED.** Only 1 still needed writing (Richarlison 17/18); 31 had already been written independently by the tail research and the Transfermarkt passes. **That also closed the verification gap: 35 pass-1 proposals were later confirmed by an independent source with ZERO contradictions.** The second source pass 1 always lacked, arrived at sideways.

---

## 3. TAG WORK , done, verified, but the numbers will drift

Cards carry descriptive tags (Goal Machine, Playmaker, The Wall...). The thresholds were **percentile-only with no absolute floor**, so small raw totals over small minutes cleared elite per-90 rates. Live symptoms: a 6-goal season held "Goal Machine"; a 1-assist season held "Playmaker".

**Fixed and shipped in `c4ad9e2`.** Raw floors added (Goal Machine ≥12 goals, Marksman ≥8, Playmaker ≥3 assists), an undocumented `×0.92` softening removed so p90 means p90, and every per-90 tag gated at ≥900 minutes. A missing stat is **exempt** from a raw floor, because **NR is not zero**.

Verified two ways: a parity harness proved the applied code matches the approved simulation on 0 of 57,234 rows differing, and the result was then **checked on the actual rendered cards**, not just in a table.

**Cards carrying at least one tag: 37.2% -> 26.0%.**

**THE PERCENTAGES ARE PROVISIONAL.** Tag thresholds are keyed to the coarse FWD/MID/DEF family, so every position reclassification moves cards between threshold tables and changes which bar a card is measured against. **Re-run the simulation once the position work settles.** Do not quote the current figures as final.

---

## 4. WHAT TO DO NEXT, in priority order

**FINISH POSITIONS FIRST** , `bigclub_batches/` is in flight. Then, in order:

1. **ENGINE RECALIBRATION.** The next real engine work.
2. **TRAJECTORY TAGS** , Peak / The Standard / Breakout / Renaissance.
3. **SEARCH BAR ON `card.html`.**
4. **GRADIENT CARD-FACE VISUAL PASS** , the contrast harness CANNOT measure gradient surfaces, so card faces are UNVERIFIED, not clean (see §C). Needs a real device, both themes, including a yellow/gold club and a white/light club.
5. **VV LOADER.**

**LAUNCH QUEUE, separate from the above:**
1. **OG / social meta (launch-blocking).** There are **zero** `og:`, `twitter:` and `description` tags on all 15 pages, so shared links unfurl with the bare `<title>` and no image. **And the titles are the whole unfurl today and several are wrong:** rankings says "Web Shell", compare says "Web Compare", preferences says "My Club" (wrong page), and all 57,234 player cards say just "VVonderXI". **Fix the titles first.** `og-image.png` exists and is a correct 1200×630 brand image.
2. **Cache-buster on `vv-core.js`.** It loads with no version token, so browsers served the OLD tag logic after the last deploy. Needs a consistent token across ~9 pages (`card.html` references it twice).
3. **Delete or rewrite the dead cron.** `vercel.json` fires `/api/refresh-players` nightly at 03:00 and that handler is a deliberate no-op.
4. **Vercel Pro before launch.** The gate is **licensing, not capacity** , Hobby is non-commercial-only. Nothing in the codebase changes when it flips.
5. **API-Football key: the "(exposed)" note is UNSUBSTANTIATED.** The key is not in HEAD, not in git history (`git log --all -S` returns 0 commits), never in any HTML, and `.env` is gitignored and untracked. No deployed function touches it. The claim traces only to "NEXT" lines with no incident recorded anywhere. **Either Lucas knows of a leak outside the repo and rotates it, or the doc should be corrected. Do not treat it as a live emergency without asking.**

---

## 5. RULES YOU MUST NOT REDISCOVER THE HARD WAY

- **Provenance does not generalise across fields.** For **position**, research (Transfermarkt, Sofascore) beats API-Football, because API-Football does not state a position , it *infers* one from a lineup grid cell, which is the root cause of the whole CAM problem. For **assists**, the opposite holds: FBref is authoritative and research is not. **Do not carry one field's source hierarchy over to another.**
- **Write on `api_player_id`, never on display name.** Names are not unique here and the collisions are live and position-changing: `J. Rodríguez` is three different players, `João Mário` two, `Nenê` two, `L. Pellegrini` two, and in every case the collision partner plays a **different position**. A name-keyed write puts a wrong position on a wrong player and looks completely plausible in the diff. The writers enforce this in code; do not bypass it.
- **When a rendered page and a measurement disagree, the render wins. Go and look.** Theme toggling animates colour, so `getComputedStyle` mid-transition returns interpolated garbage. That produced a fabricated bug report and a commit with a false number in it. Use `withTheme()` in `_audit.js` for anything theme-dependent.
- **Before overriding a style, confirm the selector actually matches the element.** Four consecutive failed fixes went to one element because every attempt targeted a selector that matched nothing.
- **A missing signal is not evidence.** An empty result can mean "broken" or "the harness cannot see this". Establish a positive control before declaring anything broken.
- **`player_card_mv` is a materialized view with a frozen column list.** Adding a column to the underlying view surfaces it nowhere; it needs a DROP + CREATE of the matview plus its 8 indexes. Always `REFRESH MATERIALIZED VIEW player_card_mv` after any data or view change.
- **Assert every string replacement landed.** A `.replace()` that matches nothing is a SILENT no-op that reports success. That stamped 7 rows with the wrong provenance while the writer printed the right one. Use `assert old in s` before replacing, grep-verify after, and prefer tools that error on a missed anchor.
- **Match `scripts/enrichment/known_players.csv` on DATE, not source.** The `knowledge` label is shared with rows dated 2026-07-04 that set cards TO CAM. Filtering on source made those read as resolved. Use `date >= 2026-08-09` for this session's writes.
- **Verify a write by READING THE FILE, not the tool's success message.** Both bugs above printed success.
- **No em-dashes or en-dashes anywhere.** Spaced commas. `NR` for missing data, never 0.

---

## 6. WHERE THINGS LIVE

```
CLAUDE.md                  source of truth. §F = session log, newest at top. WINS on any conflict.
CLAUDE_ARCHIVE.md          frozen SESSION LOG only. 87.1%; next cut is PART 1 by month at 90%.
INGESTION_RECOVERY.md      the WHOLE ingestion thread: summary + full narrative
POST_LAUNCH.md             deferred, explicitly not launch-blocking
LAUNCH_STAGE.md            UI hygiene + improvements + share/social tiers
DB_HYGIENE_STAGE.md        the DB sitting (search fixes + percentile columns share one rebuild)
ACCOUNTS_STAGE_SPEC.md     accounts/waitlist, nothing built

position_risk_report.txt      the 4 risk groups, ranked
position_bigclub_review.csv   236 big-club cards, 184 unchecked
bigclub_batches/              IN FLIGHT, 5 batches for Fable
cam_tail_batches/             PROMPT.md + the 5 batches that ran
research/cam_tail_verified/   what the research actually returned

vv-core.js                 shared renderers + the tag engine. Loaded by most pages.
scripts/enrichment/        the position writers, hold files, known_players.csv provenance
cam_tail_batches/          PROMPT.md (load-bearing) + the 5 batches that ran
research/cam_tail_verified/  what the research actually returned, evidence text included
```

**Two-terminal discipline:** Claude Code = edits, SQL, commits, and never pushes. Terminal C = reads, verification, pushing. Lucas pushes himself, always.
