# INGESTION-GAP RECOVERY , CLOSED THREAD (extracted from CLAUDE.md §E, 2026-08-09)

**CLOSED 2026-07-23.** 780 player-seasons recovered across all 9 leagues, insert-only, ONE public band crossing in total. `player_season_cards` ~56.5k -> 57,234.

**Read this file only when doing a re-ingest, an importer change, or any bulk data addition.** Extracted under the CLAUDE.md stage-file rule. **CLAUDE.md wins on any conflict.**

**THE FULL NARRATIVE IS NOW IN THIS FILE, BELOW** (sections 2.0 to 2.3, merged out of `CLAUDE_ARCHIVE.md` PART 2 on 2026-08-10 when the archive hit 98.9% of the truncation limit). Per-league recovery reports, importer guard-design history and the falsified models are all here. **`CLAUDE_ARCHIVE.md` no longer contains any ingestion content , do not go looking for it there.**

## WHAT STAYED IN CLAUDE.md (do not duplicate here, and do not assume this file is the authority on them)

Five load-bearing facts were promoted into §E of CLAUDE.md BEFORE this extraction, because they govern work that has not happened yet:
1. **INSERT-ONLY, ALWAYS** for any future re-ingest.
2. **The engine-exposure audit** , percentiles are NOT partitioned by league or season.
3. **The two live disclosure commitments** , the 300-minute coverage floor, and the source-absent marquee legends.
4. **The locked decision and its two rejections** , accept-and-disclose; partitioning REJECTED; anchor-freezing REJECTED.
5. **`api/import-players.js` is the source of truth for the guards**, not any doc.

Everything below is the full record, preserved verbatim.

---

## §E INGESTION COVERAGE GAP (full text as it stood at extraction)

- **INGESTION COVERAGE GAP , CLOSED 2026-07-23 (780 recoveries across all 9 leagues). Full narrative, per-league reports, guard-design history and falsified models: sections 2.0 to 2.3 BELOW IN THIS FILE.** Three distinct things, do NOT conflate , items 1 and 2 remain LIVE DISCLOSURE COMMITMENTS, item 3 is fixed and shipped:
  1. **~20 players/club is BY DESIGN, not a bug , DISCLOSE, do not "fix".** `api/import-players.js` applies an explicit **300-minute floor** (`MIN_MIN=300`); pagination is correct. Universal across all 9 leagues. A real PL season is ~500-560 players vs our ~400, so the fringe ~20-25% (sub-300-min) are intentionally excluded. **Launch stance: disclose the coverage boundary via confidence dots + copy. Not a blocker.**
  2. **SOURCE-ABSENT marquee legends , re-ingest CANNOT recover them. DISCLOSE.** Rooney/Gerrard/Lampard/Kompany are absent from API-Football's season-stats linkage entirely (Gerrard exists as player entity 105422, but `/players?search=gerrard&league=39&season=2013` returns 0). No re-ingest via this endpoint recovers them. **This is the real credibility hole, but it is BOUNDED** , most legends ARE present (Messi/Salah/Kane/Reus/Xavi). Post-launch mitigations only: per-player career-endpoint fallback, or an alternate source (see the parked data-source options in §D DEFERRED).
  3. **IMPORTER STAT-BLOCK BUG , FIXED + FULLY ROLLED OUT.** API-Football emits multiple same-league stat blocks per season; the old `.find(first id match)` grabbed the wrong one (usually a 0-minute phantom), failed the 300-min floor and skipped the player. Patched, then re-ingested insert-only across all 9 leagues: **780 player-seasons recovered, psc ~56.5k -> 57,234, 1 public band crossing total.**
  - **THE LIVE IMPORTER GUARDS (all thresholds in one block at the top of the guards section in `api/import-players.js` , that file is the source of truth, this is the map).** Classify same-league blocks after dropping 0-min phantoms: 0 real -> skip; 1 real -> take it; >=2 real -> ARTIFACT (dedupe to max-minutes, **NEVER sum** , summing doubles: Cunha 15g -> 30g) vs GENUINE split (sum goals+assists+minutes, team_name = dominant club). 300-min floor applies to the RESULT, not block[0]. Artifact tests: season-DERIVED apps/minutes ceilings (never a static constant , BPL runs to 45 apps, TR changes size by season) + near-mirror + **exact minute equality regardless of appearances** (a measured copy signature, 3/3 perfect association). Plus two duplication nets: **(a) PROPORTIONALITY GATE** (Poisson upper tail vs an elite ceiling, `CEIL_G90=1.00`/`CEIL_A90=0.60`/`P_IMPLAUSIBLE=0.01`, **richest block EXEMPT** so a genuine elite season is never gated for being too good); **(b) PER-STAT DUPLICATION GUARD** requiring BOTH shared nonzero stat `>=MIN_SHARED_STAT=3` AND minute ratio `>=MIN_MIN_RATIO=0.5`.
  - **RULE FOR ANY FUTURE RE-INGEST , INSERT-ONLY, ALWAYS.** The importer's default write is `upsert(onConflict:api_player_id,season,league_code)`, which REWRITES existing rows; since the view recomputes rt from goals+assists, that shifts existing scores. Use `ignoreDuplicates:true` (ON CONFLICT DO NOTHING) + bypass/clear the `isSeasonDone` checkpoints (a plain re-run does nothing otherwise), then REFRESH MATERIALIZED VIEW. Cost reference: a full 9-league pass is ~3,500-4,500 API-Football calls (~5-6% of the 75k budget), ~1-2h wall.
  - **METHOD CORRECTION , do NOT screen a dry run on the importer's `rt > 85` eyeball.** The importer's `ratingToRt` is API-match-rating based and diverges wildly from the view's output-driven rt (it flagged Clauss 89 -> view 72 and Doue 87 -> view 69 while MISSING Lepaul, the one card that actually landed in World Class). **Screen on PREDICTED OUTPUT (goals+assists vs minutes), and screen OUTFIELD ONLY** , the output proxy fails badly on GKs (Turati predicted 25, actual 74) but that is harmless, since GK is capped at 75 and can never reach the flag line. Proxy accuracy on outfielders: MAE 5.6-8.6 across leagues, precise at the top end.
  - **ENGINE EXPOSURE , READ-ONLY AUDIT 2026-07-22. THE PERCENTILES ARE NOT PARTITIONED BY LEAGUE OR SEASON. This is the single most important fact for any future data addition.** Read from `migrations/stage3_league_strength.sql` (corroborated against the live `player_card_mv` column list; a fresh `pg_get_viewdef` could NOT be pulled because the `exec_sql` RPC returns no rows , verify in the SQL editor before acting on this):
    - `pos_pct`, `posvol_pct`, `rel_pct`, `defvol_pct`, `duelq_pct` = `percent_rank() OVER (PARTITION BY s.pos ...)` where `s.pos` is the COARSE `psc.position` (DEF/MID/FWD/GK). **No league partition. No season partition.** The `scored` CTE filters only `minutes >= 300 AND goals IS NOT NULL` , it is not even limited to 2016+.
    - `abs_pct`, `absvol_pct` = `PARTITION BY (CASE WHEN pos='GK' THEN 1 ELSE 0 END)` , i.e. **ONE GLOBAL POOL of every outfielder in the database**.
    - `ref.gaw_ref` = a SINGLE GLOBAL SCALAR (99th percentile of gaw over all non-GK rows) feeding every card.
    - `anchors`: `b85`/`b90`/`b95`/`btop` = the **650th / 150th / 12th / 1st best `b` value in the ENTIRE DATABASE** (`ORDER BY b DESC OFFSET 649 LIMIT 1` etc). Adding cards changes WHICH card those are, re-scaling the whole 80-100 range.
    - ONLY the defensive pool (`pool_ingr`) is narrowly scoped: partitioned by `pp."position"` (the 8-bucket pool) and filtered `season_year >= 2016`. §E previously described ONLY this term and called it the whole story. It is not.
    - **PLAINLY: adding a Belgian midfielder moves a Spanish midfielder (same `pos='MID'` partition), moves a Spanish striker (same `abs_pct` outfielder pool), and can move ANY card above 80 (anchors). Charles 2018 is almost certainly the anchor effect , LL added two cards at rt85, pushing the 650th-best cutline and demoting a card sitting exactly on it.**
  - **BOUNDARY EXPOSURE (whole DB, 2026-07-22): 876 of 53,624 scored cards (1.63%) sit within ONE POINT of a public band boundary.** 79/80: 196 at 79 + 265 at 80. 84/85: 99 + 174. 89/90: 59 + 69. 94/95: 7 + 7. By league: PL 3.40% most exposed, LL 2.12%, BL/SA 2.05%, L1 1.30%, ERE 1.07%, TR 0.81%, BPL 0.74%, PRT 0.66%. This is the population at risk from ANY future data addition, not just this rollout.
  - **DECISION LOCKED 2026-07-22 (Lucas): ACCEPT AND DISCLOSE. FINISH THE ROLLOUT. Rollout UN-HALTED.** The band crossings are accepted as a known, disclosed property of a population-relative score, not treated as a defect to engineer away.
    - **REASONING (Lucas's, recorded verbatim in substance): the scale STABILISES as coverage completes, so stopping halfway carries the exposure AND keeps the churn ahead of us.** Half-finished coverage is the worst state , you hold all the risk of an incomplete population and still owe the remaining movement later. Finishing spends the churn once.
    - **PARTITIONING THE PERCENTILES , REJECTED. Cross-league comparability IS the product.** Partitioning `pos_pct`/`abs_pct` by league or season would kill the one thing the Index exists to do (rank a Ligue 1 season against a Bundesliga season against a 2011 season). A score that only ranks within its own league is not this product. Do not revisit without a product-level decision, not an engineering one.
    - **ANCHOR-FREEZING , REJECTED. Freezing to incomplete data locks in a known-wrong scale.** b85/b90/b95 are the 650th/150th/12th best cards in the DB; freezing them NOW would permanently encode the anchors of a database we already know is missing ~500 recoverable player-seasons. A moving-but-converging scale beats a frozen-and-wrong one.
    - **DISCLOSURE REQUIRED (this is the other half of the decision, not optional):** the VV Score is RELATIVE , it ranks a season against every season measured, and as coverage grows, rankings move. Same disclosure family as the defender-honesty note and the 300-minute coverage floor. **SHIPPED to vvindex.html "What We Measure, and What We Don't" (see §F).**
    - PRACTICAL CONSEQUENCE: expect order ~10 public band crossings across the remaining 7 leagues, concentrated at the 84/85 seam. They are CORRECTIONS toward a more complete population, not drift. Report them per league; do not silently absorb them.
  - **ROLLOUT COMPLETE 2026-07-23 , ALL 9 LEAGUES SHIPPED. PL 67 + LL 93 + L1 52 + SA 167 + TR 145 + PRT 64 + BL 35 + ERE 90 + BPL 67 = 780 recoveries.** player_season_cards ~56.5k -> **57,234**. **ONE public band crossing across all 780 recoveries (Charles 2018 LL, 85->84)** , the "order ~10" model was pessimistic; actual ~1 per 780. **Zero blank-team cards written anywhere (verified).** ERE + BPL closed clean: ERE 90 (top recovery rt67, MAE 5.6, 0 public), BPL 67 (top rt79 G. Bruno, MAE 5.7, 0 public, section-5 flagged ZERO , the derived 45-app ceiling correctly accommodated the Belgian playoff season, the exact league the old static constant would have broken). Final band census: 12 at rt>=95, 138 at 90-94, 500 at 85-89, 818 at 80-84.
  - **POST-ROLLOUT WORK REMAINING (none are launch blockers):**
    1. **Combined-score duplication guard** (design in section 2.2 BELOW IN THIS FILE) , re-resolves ~8 watch-list rows + Basacikoglu across all 9 leagues in one pass.
    2. **Blank-team re-run** , the blank-team rows skipped in the live 2025 seasons (PRT 21 + any BL/ERE/BPL) land on a plain insert-only re-run once API-Football backfills the promoted-club names. Self-healing by construction.
    3. **WRONG-BLOCK PASS , DEFERRED POST-LAUNCH (Lucas 2026-07-23). A separate gated mini-project, NOT a residual cleanup (scoped read-only 2026-07-23, PL only, NOTHING written). Parked , not awaiting a decision; the three policy questions below stay recorded but UNANSWERED, to be taken up post-launch if pursued at all.** DISTINCT from the recovery rollout: recovery INSERTED missing rows (insert-only, rt-safe); this REWRITES existing rows that captured the WRONG same-league block in the original bulk import. Mechanism: the pre-fix `.find(first id match)` stored the MINOR stint of a multi-block season; insert-only could NOT correct these (key already existed -> ON CONFLICT DO NOTHING), so they persist wrong. **PL CENSUS: 109 TRUE wrong-block rows (1.61% of 6765).** All understated (stored = minor block; correct has more minutes/goals). Examples: **Torres 2010** stored Chelsea 762m 1g -> correct Liverpool 2657m 10g (blocks Chelsea 762 + Liverpool 1895); **Carroll 2010** stored Liverpool 448m 2g -> Newcastle 2073m 13g; **D. Ba 2012** stored Chelsea 868m 2g -> Newcastle 2537m 15g; **Bent 2010** stored Villa 1400m 9g -> Sunderland 3150m 17g; **Ings 2022** stored West Ham 771m 2g -> Aston Villa 1590m 8g; **Rashford 2024** stored Villa 441m 2g/2a -> Man Utd 1424m 6g/4a. **NINE-LEAGUE ESTIMATE ~500-900** (PL is transfer-heavy so the 1.61% rate is an upper end; naive extrapolation is ~920). Stored public bands of the PL 109: 1 Generational, 2 Elite, 3 World Class, 18 Standout, 85 Accomplished , so 6 sit in NAMED public bands. **TWO BLOCKERS make this NOT a quick fix:** (A) it is rt-TOUCHING , delete-then-reinsert on live existing rows, the exact thing the rollout avoided; band moves cut BOTH ways (Rashford DOWN rt82->~72 where a hot minor-block rate inflated him; the big understatements UP). (B) it is ENTANGLED WITH THE FBref ASSISTS LAYER , stored assists for older seasons come from FBref enrichment (see §C provenance), API-Football returns 0/null, so a naive reinsert from API would CLOBBER FBref assists. Proven by the scan's false-positive class: 24 assists-only diffs (20 single-block, pure FBref-vs-API, e.g. Kane 2014 stored 21g/4a vs API 21g/0a, Aguero 2013 6a->0a, Diego Costa 3a->0a); and 20 of the 109 true cases also carry stored assists a reinsert would put at risk. **THREE POLICY QUESTIONS, RECORDED BUT UNANSWERED (revisit post-launch, if pursued):** (1) SCOPE , census all 9 leagues read-only first (~4,000 more calls) for the true total, or commit off the PL sample? (2) ASSISTS POLICY , how to avoid clobbering FBref: carry over the greater of stored/API assists, or fix minutes+goals only and leave assists, or fix then re-run the FBref enrichment over corrected rows? (3) BAND-CROSSING POLICY , fix all, or hold rows crossing a NAMED public band for individual review? Scoping script: scratchpad/residual_scope.js (read-only, per-league). If ever pursued, treat as its own gated project, same discipline as the rollout (scope -> report -> explicit go -> execute), NOT run on momentum.

    **UPDATE 2026-08-21 , THE DEFECT IS LIVE IN PL 2025/26, AND THE TEST WE WOULD REACH FOR CANNOT SEE IT.**
    **185 cards carry `card_id < 120000`, all season 2526, 181 of them PL.** Pool-versus-coarse contradiction inside the block runs **45.24% against a 12.52% baseline elsewhere.** But the contradiction rate is only the SMOKE: the rows are attached to the WRONG PLAYER, so **a row where both position fields agree is not clean, it is undetected.** `Alex Telles` is coarse GK and pool GK at Leeds over 1,980 minutes and is scored on the keeper branch, capped at 75; he is a left-back. `Eder Militao` sits at Leeds with 6 goals; `F. Chalov` is Manchester City's keeper over 3,060 minutes. `Diogo Costa` (api 369) holds Leeds 2,378 min / 5 goals AND Porto 2,907 min / 0 goals in the SAME season.
    **So the census for this pass must be CLUB-VERSUS-PLAYER, not field-versus-field** , plus minutes summing past one season across two clubs, and goals from a pooled goalkeeper. **CORRECTED SAME DAY , THE BLOCK IS SYSTEMATIC, NOT PARTIAL.** The first reading called it partial because Pickford, Leno and Verbruggen looked correct inside it. **They are not: `api 803` reads as `Jordan Pickford` here and the provider says `L. Pernica`.** Plausibility had been judged against our own `players` table, which is itself one of the corrupted layers. **10 of 12 sampled cards carry a name that is not that api id's player**, and the 2 name-matches sit at impossible clubs. A spot check reads clean because the corruption FORGES FAMILIAR NAMES, not because any part of the block is sound. **Nothing was written.**
    4. **Watch-list** (8 live: L1 1, SA 3, BL 2, ERE 2 + BPL Vaesen) , comparable-minutes + shared-small-stat rows that summed; validate against the full population before any MIN_SHARED_STAT change. Public-crossing rate so far 1 per 379 recoveries , running WELL BELOW the "order ~10" model, which projected ~3-5 from ripple plus an anchor effect. Revised expectation for the remaining 5: **order 1-3, not 10.** Caveat that keeps the model honest: crossings are driven by whether a league adds cards AT a public seam, not by recovery count, so a single league that recovers several 84-86 cards could produce them in a cluster.

    ---
    ### TRANSFER-SUM REPAIR , SCOPED 2026-08-23, NOT RUN. Decide before or after the merge.

    **THE DEFECT.** Same-league mid-season transfers are stored as ONE half on roughly two thirds of
    genuine splits. Measured against the provider across three league-seasons: **34 of 51 halved**
    (PL 2023/24 7/12, PL 2025/26 9/13, SA 2023/24 18/26). Worst seen: Semenyo 1,798 of 3,200 minutes.
    **Extrapolated ~1,600 cards across 144 league-seasons , an ORDER from a three-season sample, NOT a count.**

    **THE CAUSE IS STALE DATA, NOT THE CODE.** Run against real provider blocks, the current resolver
    sums 49 of 51. The stored rows were written by an earlier summing path. **So this is a RE-RUN,
    and no further resolver change is required** (the one real defect, `sharesStat` firing on a single
    matching stat, is fixed).

    **WHY A PLAIN RE-RUN WILL NOT DO IT.**
    `--insert-only` cannot repair a halved row , ON CONFLICT DO NOTHING leaves it exactly as it is.
    Default mode DOES rewrite it, but it rewrites **all ~57,000 rows** from today's provider data,
    folding in every unrelated drift since each row was written. That is a far larger change than the
    ~1,600 rows that are actually wrong, and it is the blanket rewrite §C tells us not to reach for.

    **THE PROPOSED SHAPE , TARGETED, in the same staging as the 2026-08-21 engine pass.**
    1. **Identify externally.** Fetch all 144 league-seasons' squads from the provider and find players
       with 2+ clubs in the SAME league-season. **~5,800 calls** (144 x ~20 clubs x ~2 pages), roughly
       **90 minutes** at the 320ms delay, **~8% of the 75k budget**. Identification MUST be external ,
       our own rows cannot reveal a half we never stored (see §C, a check that reads our own data).
    2. **Classify.** Separate genuine splits from duplicated blocks and from ADJACENT-SEASON BLEED.
       The bleed is real and must not be summed: Smith Rowe 44 apps, Osula 51 apps, both above the
       38-game cap. `isArtifact` (A2) already rejects those and is correct to.
    3. **Full before-snapshot** of all rows, written and read back off disk.
    4. **Update ONLY the affected rows**, recomputing through `resolveSeasonStat` so the repair uses the
       same code path as an import rather than a second implementation.
    5. **Refresh the matview, then assert**: row count unchanged, null-rt count held, rt range held,
       band counts 12 / 150 / 650 (rank anchors hold BY CONSTRUCTION), and a full before/after diff
       listing every card that moved and every public band crossing.

    **COST BEYOND THE CALLS, and this is the part that decides the timing.** Minutes drive the
    availability term and EVERY per-90 rate, so ~1,600 cards change on both. Percentiles are global
    and unpartitioned (see §E engine exposure), so **the ripple reaches cards that were never touched**
    and band OCCUPANCY moves even though the counts hold. Expect public band crossings and expect to
    have to explain them. **This is the same blast radius as the WRONG-BLOCK pass and belongs beside it.**

    **IT GROWS WHILE IT WAITS, SLOWLY.** The live season is still being imported, so every pass keeps
    adding halved cards and the ~1,600 rises. **The `sharesStat` fix reduces the rate but does not stop
    it** , that guard accounted for 1 of 3 resolver misses, and the rest of the halving was never the
    resolver at all. So the population drifts upward until the repair runs. **It drifts slowly: a
    league-season yields on the order of ten, and only the live season is still being written.**

    **STATUS: POST-MERGE, and the reason is the KIND of wrongness, not the size.** These cards
    **UNDERSTATE a real player-season** , the name, club and identity are right and only half the
    minutes are present. **The BSD block was different in kind: those cards MISIDENTIFIED, carrying one
    person's season under another person's name**, which is why it was worth writing against live data
    before launch and this is not. A visitor reading a halved card sees a true season told short; a
    visitor reading a block card saw a fiction. **Understating is a data-quality debt the confidence
    dots already disclose. Misidentifying is a credibility hole.** Ship on the first, never on the second.

    **RECOMMENDATION: AFTER the merge.** It is not a correctness blocker for launch , the cards are
    understated, not wrong about who they describe , and running it before the merge adds a large,
    hard-to-review rt movement to a QA pass that is already gating 167 files. **The cards it fixes are
    mostly mid-table**, since a player who moved mid-season rarely tops the ladder.


---

# THE FULL NARRATIVE , merged from `CLAUDE_ARCHIVE.md` PART 2 on 2026-08-10

Everything above this line is the SUMMARY layer, extracted from CLAUDE.md §E on 2026-08-09.
Everything below is the original archived narrative, moved here verbatim when
`CLAUDE_ARCHIVE.md` reached 98.9% of the 150k truncation limit and had no headroom for the
next session-log archive pass. **Nothing was edited in the move except the section heading
level.** The two layers are complementary: read the summary for what was decided, read the
narrative below for the measurement behind it.

**Section 2.2 is referenced by an OPEN work item above** (the combined-score duplication
guard, still unbuilt). That is the one part of this narrative that is not purely historical.

**CLOSED TRACK. 780 player-seasons recovered across all 9 leagues, insert-only, 1 public band crossing total. Do not reopen pre-launch.** CLAUDE.md §E keeps the load-bearing spine of this work: the gap taxonomy and its disclosure commitments, the ENGINE EXPOSURE audit (percentiles are NOT partitioned by league or season), the BOUNDARY EXPOSURE census, the LOCKED accept-and-disclose decision with its two rejected alternatives, and the four remaining post-rollout items. What follows is the supporting evidence, the per-league shipping reports, the guard-design history, and the models that were falsified along the way.

**Read this when:** you are about to add data to the DB and need to know what moves and why; you are revisiting the importer's duplication guards; or you want the measurement behind a claim CLAUDE.md states as settled.

**Kept in original chronological order** (the rollout as it ran), not newest-first.

## 2.0 , The original three-way gap diagnosis, long form (2026-07-21)

**Condensed into CLAUDE.md §E.** Items 1 and 2 survive there as live disclosure commitments; item 3 is fixed and shipped. This is the original diagnostic detail, including the block-selection design as it was locked BEFORE it was written (the shipped guards differ , see 2.1 and 2.2 for the refinements, and `api/import-players.js` for what actually runs).

  1. **~20 players/club is BY DESIGN, not a bug , DISCLOSE, do not "fix".** Mechanism CONFIRMED in `api/import-players.js`: correct pagination (`do{}while page<=totalPages`) + an explicit **300-minute floor** (`MIN_MIN=300`, skips sub-300-min seasons). Universal across all 9 leagues (avg 17.6 ERE to 21.7 LL; range 1-37 per team-season, so NOT a hard cap). Real PL season ~500-560 players vs our ~400 => the fringe ~20-25% (sub-300-min) are intentionally excluded. Launch stance: disclose the coverage boundary via confidence dots + copy; not a blocker.
  2. **SOURCE-ABSENT marquee legends , re-ingest CANNOT recover.** Diagnostic: API-Football `/players?league=39&team&season` returns FULL rosters (27-36 players) but Rooney(ManU 2013)/Gerrard(LFC 2013)/Lampard(CFC 2012)/Kompany(MC 2013) are NOT in them. Gerrard EXISTS as a player entity (id 105422) but `/players?search=gerrard&league=39&season=2013` = 0 results => the season-STATS linkage is absent at source. No re-ingest via this endpoint recovers them (same empty response). Mitigation options (all post-launch): per-player career-endpoint fallback (uncertain payoff), an alternate source (FBref/StatsBomb/Sportmonks , see parked data options), or simply DISCLOSE. This is the real credibility hole but it is BOUNDED (most legends ARE present: Messi/Salah/Kane/Reus/Xavi/etc.).
  3. **IMPORTER STAT-BLOCK BUG , RECOVERABLE, likely affects many mid-season transfer/loan players.** Diagnostic: `/players?id=161948&season=2024` (Delap) returns his Ipswich PL season present at source (37apps/2612min/12g) , BUT his 2024 row carries MULTIPLE PL(id 39) stat blocks ("Manchester City|PL|null" listed BEFORE "Ipswich|PL|2612/12"). The importer's `(row.statistics||[]).find(x=>x.league?.id===39)` grabs the FIRST id=39 block (Man City, null minutes) => `<300` => player SKIPPED, real Ipswich season never seen. SCALE (sampled PL2024+PL2018+SA2022, 232 API calls): ~6.5% of rows have multiple same-league blocks; ~1% are DROPPED by the bug (first block fails the floor) , extrapolates to ~300-600 dropped player-seasons, HEAVILY skewed to 2020+ (PL2018 dropped 0; API-Football only started emitting the multi-block shape recently). Recognizable victims: K.Phillips, Sessegnon, Adarabioyo, Hutchinson, Miranchuk, Simeone.
     **BLOCK-SELECTION FIX (design LOCKED 2026-07-21, NOT yet written , code held per Lucas):** classify a player's same-league blocks after dropping 0-min phantoms: 0 real -> skip; 1 real -> take it (recovers Delap); >=2 real -> ARTIFACT vs GENUINE. **ARTIFACT** (API duplicates the SAME season under the player's next-season club; 46 of 56 two-real cases) = `Σminutes > league ceiling` OR near-mirror (Δmin<=40 & Δapps<=1 & Δgoals=0 & Δassists=0) -> DEDUPE to the max-minutes block, NEVER sum (summing doubles: Cunha 15g->30g, catastrophic rt inflation). **GENUINE split** (~10 of 56, distinct stints, sum <= ceiling) -> SUM goals+assists+minutes+granular into ONE row, team_name = dominant (max-minutes) club. Apply the 300-min floor to the RESULT, not block[0]. Ceiling is LEAGUE-AWARE (38-game PL/LL/SA/L1 = 3420; 34-game BL/ERE/PRT = 3060; + cushion). Union of the two artifact tests is required: ceiling alone MISSES sub-ceiling copies (Kean 990/1006 6g/6g -> would wrongly become 12g); mirror alone needs the ceiling backstop. No genuine split exceeds its ceiling, so ceiling-fired => artifact with zero false-positives. LAUNCH CHOICE: max-minutes-only is the safe v1 (recovers all DROPPED incl. Delap, CANNOT inflate rt); genuine-split summing is the correct-but-v2 refinement (needs the artifact filter validated). Proof of value: Rashford 24/25 is CURRENTLY stored as his minor loan half (Aston Villa 441m 2g rt53); summed = Man Utd 1424m 6g 4a ~rt72. Caputo 22/23 Empoli 1719m 5g rt64 -> summed 2818m 6g 8a ~rt77.
  - **RE-INGEST COST + rt-SAFETY (for recovering #3, and whatever #2 the source now has):** ~3,500-4,500 API-Football calls (56.5k cards / ~0.8 floor-survival / 20-per-page), **~5-6% of the 75k budget**, ~1-2h wall (320ms pacing + latency + 429 backoff). **NOT additive as written , two blockers:** (a) `isSeasonDone` skips completed league-seasons (import_progress all complete) so a plain re-run does nothing; forcing re-processing re-pulls all pages; (b) the write is `upsert(onConflict:api_player_id,season,league_code)` which REWRITES existing rows (goals/assists/minutes) => since the live view recomputes rt from goals+assists, existing players' **rt would shift**. To honour "existing rt must not change": make it INSERT-ONLY (`ignoreDuplicates:true` => ON CONFLICT DO NOTHING) + bypass/clear the checkpoints, then REFRESH MATERIALIZED VIEW. New player-seasons land, existing rows (and their rt) untouched. Also fix the stat-block selection (#3) before re-running or the same transfer players get skipped again.
  - **DECISION FRAMING:** #1 disclose (not a blocker). #3 is a cheap, high-value, rt-safe fix (importer patch + insert-only re-ingest) , strongest pre-launch candidate. #2 is source-limited , disclose for launch, alternate-source is post-launch.

## 2.1 , Recovery mechanics, rt-safety, and the summed-path bug (2026-07-21 to 2026-07-22)

  - **rt IS POPULATION-RELATIVE , "existing rt never moves" is IMPOSSIBLE by design, and that is CORRECT (verified live PL 2024, 2026-07-21).** Insert-only keeps existing ROWS byte-identical (confirmed: the 22 recovered were the only api_player_ids ever written; ON CONFLICT DO NOTHING + pre-skip), but the VIEW recomputes rt from population-relative terms, so refreshing moves some existing scores. TWO terms in player_card_view (see migrations/stage2_bounded_def_bestof.sql): (PRIMARY, drove all observed shifts) the `team_def` CTE , `team_def90 = SUM(team tackles+int+blocks)*90 / SUM(team minutes)` grouped by (team_id, season_year, league_code); `def_share = player_def90 / team_def90`. Adding a player to a team-season changes that denominator for EVERY teammate => their def_share => def_share_pct => def_core => rt. TEAM-LOCAL. (SECONDARY, empirically ~0) `percent_rank() OVER (PARTITION BY pool)` , def percentiles ranked GLOBALLY within position pool (all 2016+ outfielders, no league/season partition); a tiny global ripple that rounded to zero in the PL-2024 test (only teammates moved). LIVE PL-2024 RESULT: 22 recovered -> 14 existing rows moved, EVERY ONE a teammate of a recovered player, ALL defensive pools (CB/FB/CDM/CM), ALL exactly +-1, ALL mid-range (49-68), ZERO band/badge crossings. Direction is mechanical: Ipswich gained 4 low-defensive-output attackers -> team_def90 DROPPED -> teammates' def_share ROSE -> all +1; clubs that gained mids/attackers -> mostly -1. **This is a CORRECTION, not drift: completing a team's squad makes team_def90 reflect the real roster, so every teammate's def_share becomes MORE accurate.** It is intrinsic , ANY data addition (the 25/26 dup-team fix already did this) perturbs team_def90 + pool percentiles the same way; strict rt-immutability is fundamentally incompatible with a team-relative + pool-relative engine unless you freeze those denominators (which defeats the recalibration). MODEL for a full 9-league recovery (~300-600 recovered players): rate ~0.64 existing +-1 flips per recovered player (PL2024: 14/22) => **~200-400 existing rows move +-1**, ALL defenders who are teammates of recovered players, ALL +-1, ALL mid-range, NO band/badge crossings (def_core lives mid-range and structurally cannot reach 80+). ~0.4-0.7% of the 56k dataset. DECISION (Lucas, 2026-07-21): **ACCEPTED as a good trade , more-accurate scores for ~300-600 real players recovered; +-1 with zero band crossings is noise.**
  - **RECOVERY ROLLOUT STATUS (2026-07-21):** importer patch committed (3a53cc3). PL 2024 recovered LIVE + KEPT (22 rows incl. Delap Ipswich 2612m 12g 2a rt80; matview refreshed). PL ALL SEASONS rolled out (commit chain to 1b74b6e; 45 more recovered, 67 total PL; existing-rows check CLEAN , 56/6720 moved, all +-1, ZERO band crossings). LL HELD , NOT started (bug below).
  - **SUMMED-PATH BUG , FOUND in PL verification 2026-07-21, FIX + REMEDIATION PENDING (do NOT roll out further until fixed).** The genuine-split SUM path over-fires: when API-Football duplicates a player's output onto a SECOND same-league block (a low-minute phantom/early-stint block carrying the SAME goals/assists as the real block), the two blocks have DIFFERENT minutes -> the mirror test (which requires Δmin<=40 AND Δg=0 AND Δa=0) does NOT fire -> the row is classified GENUINE SPLIT -> output is SUMMED -> DOUBLED. Found by the rt>85 eyeball + a full 45-row source rescan (scratchpad/scan45.js): of 24 summed PL rows, **5 corrupt**: **M. Gibbs-White 2022 (Wolves 180m 8a + Forest 2977m 8a -> stored 16a, rt86 , EGREGIOUS, was in World Class band)**; El Ghazi 2021 (Everton 11m 1a phantom -> 2a, rt47); Matheus Nunes 2023 (-> 4a, rt57); Sturridge 2017 (-> 2a, rt53); Matt Jarvis 2015 (-> 2a, rt53). ALSO IMPLICATES THE 2024 BATCH already live: **Chilwell** (Chelsea 262m 1g + Palace 253m 1g, near-identical -> stored 2g, likely a doubled 1g , earlier "validated genuine" call was too hasty) and possibly **Ferguson**. PROPOSED FIX (design change to 3a53cc3, NOT yet written): (a) PER-STAT DUPLICATION GUARD , if two blocks share identical goals AND identical assists (nonzero), treat that output as duplicated -> dedupe to the richest block, do NOT sum (worst case under-counts a genuine 1+1 by 1, far better than doubling); (b) PER-90 PLAUSIBILITY GATE , drop a low-minute block whose per-90 output is impossible (e.g. Gibbs-White 8a/180m = 4.0 a/90). Then re-resolve the affected summed rows. REMEDIATION of live rows (all deletable by key, insert-only): delete the 5 suspect + re-check Chilwell/Ferguson, OR delete all summed recovered rows and re-insert after the fix. **INTERIM STATE (7 corrupt DELETED 2026-07-21):** the 7 duplication-suspect rows were guarded-deleted by card_id (matched on name+season+exact g/a; exactly 7 removed, PL psc 6765->6758, no other row touched) + matview refreshed. **60 clean PL recoveries REMAIN (incl. Delap Ipswich 2024 rt80 12g).** The 7 are **PENDING RE-INSERTION after the per-stat-duplication-guard + per-90 plausibility fix lands** , DELETED (card_id): Gibbs-White 2022 (186852), El Ghazi 2021 (186855), Matheus Nunes 2023 (186846), Sturridge 2017 (186862), Matt Jarvis 2015 (186868), Chilwell 2024 (186828), Ferguson 2024 (186831). Note the deleted card_ids will NOT be reused on re-insert (new serial ids). LL still held. NEXT: write the fix, re-verify the 60 clean rows are unaffected, re-resolve + re-insert the 7 (Gibbs-White should land ~5g/8a not 16a), then resume rollout LL onward.
  - **RE-RESOLVE VERIFICATION 2026-07-22 (read-only, 63 API-Football calls, NO writes; guards written but NOT committed, nothing re-inserted yet).** All 7 deleted rows re-pulled from live source + re-resolved through the patched resolver; all 60 surviving rows re-resolved OLD-vs-NEW. Four findings:
    1. **FERGUSON 2024 WAS A FALSE-POSITIVE DELETE , the record must say so.** Evan Ferguson (api 129643, NOT Nathan Ferguson api 127605 who returns a single 0-min Palace block) has West Ham 150m 0g/0a + Brighton 261m 1g/0a , a REAL Brighton->West Ham loan split. Old code and new code both resolve it to summed Brighton 411m 1g/0a, **identical**. Nothing was ever doubled: the two blocks differ on goals (0 vs 1) and both assist figures are 0, which the guard excludes. The §E line above listing him as "possibly Ferguson" corrupt resolves to **NO**. He was deleted on suspicion alone and should be re-inserted unchanged. 6 of 7 re-insert; **Chilwell is the only one that does not** (dedupes to Chelsea 262m, below the 300-min floor, so he produces no card at all).
    2. Gibbs-White CONFIRMED from source: Wolves 180m 0g/**8a** (2 apps, 4.00 a/90) + Forest 2977m 5g/8a. Per-90 gate drops the Wolves block, leaving ONE block, so he resolves through the plain `single` path -> **5g/8a**, as predicted. Others: El Ghazi -> Villa 383m 1g/1a; Nunes -> Man City 661m 0g/2a; Sturridge -> Liverpool 431m 2g/1a; Jarvis -> Norwich 358m 1g/1a.
    3. **BLAST RADIUS ON THE 60 CLEAN ROWS = ZERO.** 19 of the 60 currently resolve `summed`; the new guards re-classify **0** of them (60/60 fetched, 41 single + 19 summed, unchanged under both rules). Reason: 17 of the 19 have minor blocks carrying 0g/0a , nothing to match on and nothing summing can inflate , and the 2 with output (Cedric Soares 2019 Arsenal 250m 1g, Bony 2014 Man City 296m 2g) do not match their major block (0g and 9g respectively). **CORRECTION to this session's earlier claim that "the guard fires on every multi-block case":** that was read off the 7 deleted rows, a sample SELECTED for being duplication suspects. On the unbiased population it fires 0 times. (Also discard a transient "28 rows differ" reading , that was a null->0 coercion in the throwaway report script, not a behaviour change; the resolver returns single/deduped blocks untouched so NULL is preserved.)
    4. **PER-STAT GUARD USES OR, NOT AND.** The proposed fix above says "identical goals AND identical assists" , that formulation is WRONG and would miss the case that found the bug: Gibbs-White matches on assists only (goals 0 vs 5), so an AND test never fires. Implemented as OR (identical NONZERO goals OR identical NONZERO assists). Zero is excluded deliberately: two blocks both showing 0g agree about nothing.
  - **PROPORTIONALITY REFINEMENT , RAISED, MEASURED, DECIDED + IMPLEMENTED 2026-07-22 (Lucas: "take the refinement").** Question: is "is this block's output plausible FOR ITS OWN MINUTES" a better discriminator than identical-stat matching? Measured with a Poisson upper tail P(X>=k) at an elite ceiling (1.00 g/90, 0.60 a/90) over the 7 deleted + the §E artifacts + the 2 output-carrying summed rows. **Answer: it is decisively better on ONE of the two copy shapes and BLIND to the other , they are different axes, not competing versions of the same test.** (A) LOW-MINUTE COPY (output stapled to a short block): proportionality separates by ~4 orders of magnitude , Gibbs-White p=3.7e-5 vs the genuine short stints Nunes 3.4e-1 / Jarvis 3.9e-1 / Sturridge 5.4e-1 , far cleaner than identical/not-identical, and minutes-aware instead of a cliff at 600m. (B) FULL-BLOCK COPY (same season under two clubs, comparable minutes): proportionality CANNOT see it , Cunha 15g/2600m p=1.0, Kean 6g/990m p=0.96, both individually plausible, and these are the catastrophic 15g->30g cases. Only identical-stat/mirror/ceiling catches those. SECOND FINDING: an identical-stat match is only as informative as the stat is LARGE , identical 8a is overwhelming evidence, identical 1a is weak (1 assist is a common value, so the coincidence is cheap). The minute RATIO separates the classes cleanly in the sample: every genuine short stint sits at ratio <=0.27, both full-block copies at >=0.98. **LOCKED RULE (written, verified, NOT yet committed , thresholds all live in one block at the top of the guards section in `api/import-players.js`):**
    - gate (a) PROPORTIONALITY replaces the fixed per-90 caps: `CEIL_G90=1.00`, `CEIL_A90=0.60` (elite-season ceiling rates), drop a block when the Poisson upper tail P(X>=observed) at that ceiling is `< P_IMPLAUSIBLE=0.01`. **The RICHEST block is EXEMPT from the gate** , a genuine elite season also posts a low tail probability, so without the exemption the gate would drop output for being too GOOD rather than un-earnable. That exemption is what removes the need for any fixed minutes cliff (the old `LOW_MIN_BLOCK=600` is GONE; the tail test is minutes-aware by construction).
    - guard (b) NARROWED, requires **BOTH** (Lucas's formulation , note AND, not the OR first sketched): shared nonzero stat `>= MIN_SHARED_STAT=3` **AND** minute ratio `>= MIN_MIN_RATIO=0.5`. Rationale: an identical stat is only as informative as it is large; identical 8a/15g is near-conclusive, identical 1a is a common value and proves nothing.
    - RESULT on the 7 (live source, re-resolved 2026-07-22): Gibbs-White `single` Forest 2977m **5g/8a** (Wolves 180m 8a gated, p=3.7e-5); Nunes/Sturridge/Jarvis RESTORED as genuine splits (Man City 841m 0g/4a; Liverpool 547m 2g/2a; Norwich 431m 1g/2a); Ferguson unchanged Brighton 411m 1g/0a; El Ghazi REVERSED to summed Villa 394m 1g/2a (accepted , 11m/1a is odd but p=0.071 is not impossible and the source cannot settle it). **CHILWELL ALSO REVERSED , consequence of AND that was NOT in the original candidate:** shared 1g is below MIN_SHARED_STAT so (b) no longer fires despite ratio 0.97, and the pre-existing mirror test misses him (Δapps=4 > 1, real source is Palace 253m 8app vs Chelsea 262m 12app). He now SUMS to Chelsea 515m **2g**, clearing the 300-min floor instead of dropping out. Consistent with the same principle, but it means **all 7 re-insert** (was 6).
    - VERIFIED: the 60 clean PL rows re-resolved under the refined rule = **0 shape changes** (41 single + 19 summed, identical before and after; 60/60 fetched). Cunha (15g, ratio 1.00) and Kean (6g, ratio 0.98) still caught. Delap still recovers, Rashford-shape genuine splits still sum.
    - RESIDUAL RISK, disclosed: the truth for the 1-assist cases is UNVERIFIABLE from this source. They are real loan stints (Nunes Wolves->Man City Sept 2023, Sturridge Liverpool->West Brom Jan 2018, Jarvis West Ham->Norwich 2015) but whether the shared assist is real or copied cannot be settled here. The rule now errs toward SUMMING these; the previous rule erred toward dropping them. Both are defensible; this one is more accurate on every case where truth IS establishable.
  - **RE-INSERTION EXECUTED 2026-07-22 (DB WRITE, matview refreshed). The summed-path remediation is CLOSED , PL is whole again at 67 recoveries.** Patch committed `e311cd3` (NOT pushed). All 7 re-inserted as card_id **186878-186884** (fresh serials; the deleted 186828-186868 ids were NOT reused, as predicted). Pure INSERT: `.insert()` not upsert, all 7 keys verified absent first, players/teams looked up ONLY (never upserted) so the write touched exactly one table. PL psc 6758 -> **6765**.
    - **VIEW rt (post-refresh):** Gibbs-White 2022 Forest 2977m 5g/8a -> **rt 80 (Exceptional/"Standout")** , down from the corrupt rt86 World Class, which was the whole point. El Ghazi 47; Nunes 57; Sturridge 53; Jarvis 53; Chilwell 55; Ferguson 31.
    - **EXISTING-ROW DIFF vs pre-write snapshot (6758 rows): 11 moved, ALL exactly +-1 (8 up, 3 down), 0 vanished.** Two mechanisms, cleanly separable and BOTH as §E's model describes , but the mix is NEW information: **7 are team-local** (6 = Nottingham Forest 2022, teammates of Gibbs-White; 1 = Brighton 2024, Ferguson's teammate Hinshelwood) via the team_def90 denominator; **4 are NOT teammates of any recovered player** (Yoshida 2013 Southampton, Lascelles 2015 Newcastle, Schneiderlin 2016 Everton, Lerma 2018 Bournemouth) , that is the GLOBAL pool-percentile ripple, the secondary term §E measured as "empirically ~0" in the PL-2024 test. **It is NOT zero. Correct the model: the secondary term is small but real.**
    - **RATE IS ABOVE THE MODEL , disclose, do not bury.** 11 flips / 7 recovered = **1.57 per recovered player vs the modelled 0.64** (PL2024's 14/22). Cause is understood and not alarming: the 0.64 came from a batch of mostly low-minute recoveries, whereas Gibbs-White's 2977m is a NEAR-FULL season dropped into Forest 2022, which shifts that team_def90 denominator far harder. **Implication for the remaining 8-league rollout: scale the ~200-400 estimate by recovered MINUTES, not by recovered row count.**
    - **BAND CROSSINGS: 1, NOT 0 , the earlier "zero crossings" claim does not survive contact.** C. Kouyate 2022 Forest 57->58 crosses the INTERNAL Good/Very Good seam at 58. It is **publicly invisible** (both sit inside the grouped public "Accomplished" band = 79 and under, per vvindex/playbook), so no public band label and no badge changes , but the honest statement is "1 internal crossing, publicly invisible", not "zero". The structural claim that def_core cannot reach 80+ still holds; the crossings that matter (the 4 named public bands + 2 prestige badges) remain untouched.
  - **LL ROLLOUT EXECUTED 2026-07-22 (DB WRITE, matview refreshed) , then ROLLOUT HALTED BY LUCAS pending a decision. DO NOT START SA/BL/L1/PRT/ERE/BPL/TR.** 93 recovered, LL psc 6939 -> 7032, 0 errors, 0 duplicate team rows created (all 25 club names matched an existing `teams` row exactly , the Brighton/"Brighton & Hove Albion" alias mechanism did NOT fire; LL-wide check found no club under >1 name). Also carries the appearances-ceiling fix (commit 83b28e7): LL Robert Navarro 2024 was summing Mallorca 1285m/23ap + Athletic Club 1286m/25ap into a **48-appearance card in a 38-game league**; the new `Σapps > league_games + 2` test dedupes him to 1286m 1g. Recovery skew 92% 2020+ (44 in 2024 alone). New rows: 81 scored, **12 UNSCORED (null rt)** , every one has `goals IS NULL`, and `player_card_view`'s `scored` CTE filters `WHERE psc.goals IS NOT NULL`, so a null-goals card gets no rt at all. NOT caused by this work (LL already held 323 such rows; DB-wide 2990 of 56614 cards are unscored) but it means 12 of 93 recoveries land as scoreless cards. Top new rows: Sorloth 2024 Atletico 1560m 20g/2a **rt85** and Ayoze Perez 2024 Villarreal 1976m 19g/2a **rt85** , both `single` shape, both real seasons, both legitimate World Class entries.
  - **WHAT THE LL PASS FALSIFIED , four corrections to the model recorded above. Read these before trusting any earlier §E claim about rt safety.**
    1. **"ALL +-1" IS FALSE.** 102 existing rows moved: {+1: 60, -1: 38, **-2: 4**}. The four -2s are all Osasuna 2024, whose team-season gained **F. Boyomo 3213m CB** , a near-ever-present defender. Magnitude scales with the recovered player's MINUTES, not with row count. Expect -2 wherever a full-season defender is recovered.
    2. **"ZERO PUBLIC BAND CROSSINGS" IS FALSE , and the STRUCTURAL ARGUMENT FOR IT IS RETIRED.** 4 crossings, 3 internal (invisible inside the grouped "Accomplished" field) and **1 PUBLIC: Charles 2018 Eibar ST 85 -> 84, World Class -> Standout.** He has NO recovered teammate (Eibar 2018 gained nobody), so this is NOT the team_def90 mechanism. **RETRACTION: the claim "def_core lives mid-range and structurally cannot reach 80+, so the bands that matter are untouched" (recorded twice above) does NOT hold and must not be relied on again.** def_core not reaching 80+ is true and irrelevant: a card already sitting ON 85 needs only a one-point nudge, and the global terms below supply it. Boundary-sitters are exposed no matter where def_core lives.
    3. **THE GLOBAL RIPPLE IS 27% OF MOVEMENT, NOT "~0".** Mechanism split: 74 team-local (teammate of a recovered player) vs **28 global**. §E's original "(SECONDARY, empirically ~0)" reading came from one small PL batch and is retired.
    4. Flip rate 1.10 movers/recovery (PL 1.57, original model 0.64). Rate tracks recovered MINUTES; row-count models under-predict.

## 2.2 , Per-league rollout reports and guard refinements (2026-07-22 to 2026-07-23)

  - **L1 SHIPPED 2026-07-22 (DB write, matview refreshed): 52 recovered, psc 6517 -> 6569, 0 errors, live counts matched the dry run season-for-season.** 73 existing rows moved, **ALL +-1** (no -2 , no full-season defender was recovered into a single team-season the way LL's Boyomo was). **8 band crossings, ALL INTERNAL at the 57/58 Good/Very Good seam, ZERO PUBLIC.** Mover rt range 34-64, i.e. nowhere near the 79/80 or 84/85 seams , that, not luck, is why nothing crossed publicly. Mechanism split 64 team-local / 9 global ripple (12%, vs LL's 27%). Flip rate 1.40/recovery. 3 of 52 land unscored (null goals).
    - **E. Lepaul 2025 Rennes 2702m 21g/5a -> view rt 87, WORLD CLASS, ranks #24 of 6277 scored L1 cards.** Highest-output summed row the fix has produced. VERDICT: sound. The genuine Angers->Rennes split contributes only 175m and 1 goal; the card is overwhelmingly the Rennes season (2527m 20g), so had it deduped instead of summed it would sit at essentially the same rt. **The summed path is not doing the heavy lifting on this card** , which is the reassurance worth having, given a top-band card produced by summing was the original failure mode (Gibbs-White rt86 off a doubled 16a).
    - **METHOD CORRECTION , the dry-run "rt > 85" eyeball screen is UNRELIABLE and must not be trusted alone.** It uses the importer's crude `ratingToRt` (API match-rating based), which diverges wildly from the view's output-driven rt: it flagged Clauss (importer 89 -> **view 72**) and Doue (87 -> **view 69**) while completely MISSING Lepaul, the one card that actually landed in World Class. For future dry runs, screen on PREDICTED OUTPUT (goals+assists vs minutes), not on importer rt. Lepaul was caught only because Lucas called him out by name off the dry-run block list.
  - **WATCH-LIST CLASS (opened 2026-07-22, NOT acted on , do NOT change thresholds mid-rollout):** summed rows where the blocks have COMPARABLE MINUTES (ratio >= MIN_MIN_RATIO 0.5) AND share a nonzero stat, but the stat is BELOW `MIN_SHARED_STAT`=3 so guard (b)'s AND declines to fire. These sum; if they are copies we are doubling a small number. **Growing across leagues: LL 0, L1 1, SA 3.** Named instances , **SA J. Karlsson 2024** (Bologna 297m/7ap 1g + Lecce 296m/17ap 1g, ratio **1.00**, the most suspicious: a ONE-MINUTE gap with identical goals, though the sharply different apps 7-vs-17 argues against a straight copy); **SA E. Vignato 2022** (Bologna 238m/8ap 0g/1a + Empoli 245m/15ap 0g/1a, ratio 0.97); **SA A. Belotti 2025** (Como 147m/2ap 2g + Cagliari 233m/8ap 2g, ratio 0.63); **L1 S. Grandsir 2018** (ratio exactly 0.50, shared 1 assist). REVISIT `MIN_SHARED_STAT` ONCE THE FULL POPULATION IS VISIBLE (after all 9 leagues), not before , changing it mid-rollout would make leagues incomparable and re-open rows already shipped. Evidence needed before touching it: whether the class keeps scaling with league size, and whether any instance can be independently confirmed as a copy.
  - **SA SHIPPED 2026-07-22 (DB write, matview refreshed): 167 recovered, psc 6891 -> 7058, 0 errors, live counts matched the dry run season-for-season.** Biggest league yet (nearly 2x LL) and the FLATTEST skew , only 69% 2020+ (LL 92%, L1 75%), with real depth in 2012 (11) and 2016 (9), so the multi-block shape is NOT purely a recent-API artefact in Serie A. Shapes 78 single / 87 summed (52.1%, highest yet) / 2 deduped; proportionality gate fired 0 times. 9 of 167 land unscored (null goals). **115 existing rows moved, ALL +-1, 0 vanished. 4 band crossings, ALL INTERNAL, ZERO PUBLIC** , mover rt range 16-74, again nowhere near a public seam. Ripple share 17%. **Flip rate 0.69/recovery , the LOWEST yet** (PL 1.57, L1 1.40, LL 1.10), consistent with the "scales with recovered MINUTES not row count" correction: SA recovered many low-minute rows.
    - **M. Kean 2024 Fiorentina 2710m 19g/3a -> rt87 WORLD CLASS** (`single` shape, no summing). De Ketelaere 2023 Atalanta 2040m 10g/8a -> rt81. **The predicted public crossing did NOT materialise** , SA had 109 cards at rt>=85 and adding one at 87 did not demote a boundary-sitter this time. Do NOT read that as safety: it is one observation, and LL showed the anchor effect can bite.
    - **Esposito 2025 landed as intended by the ceiling fix: `deduped` Cagliari 2705m 36ap 7g/5a rt76** , no 40-appearance card shipped.
  - **OUTPUT-PROXY VALIDATED ON A SECOND LEAGUE, with one KNOWN BLIND SPOT.** SA MAE **8.6** across 158 scored recoveries (L1 backtest 7.3). At the top end it is precise where it matters: **Kean predicted 86 / actual 87; De Ketelaere predicted 82 / actual 81.** **BLIND SPOT , GOALKEEPERS: the proxy is output-based and the view scores GKs on a separate minutes-driven branch, so it fails badly on them** (SA Turati 2023 predicted 25, actual 74 , a 49-point miss; GK subset MAE 11.7 vs outfield 8.4). **This is HARMLESS for the screen's purpose: GK is capped at 75 (§C) so a keeper can never reach the 85 flag line** (max GK among SA recoveries was 74). Rule going forward: **apply the proxy to OUTFIELD players only; screen GKs not at all, since they cannot enter the top band.**
  - **PRT SHIPPED 2026-07-23 (DB write, matview refreshed): 64 recovered, psc 5943 -> 6007, 0 errors.** Flattest skew of the rollout (64% 2020+). **Taremi 2020 FC Porto 2226m 17g/10a -> rt82 (Standout, NOT World Class)** , the proxy predicted 82, actual 82, exact. 0 new rows at rt>=85. 64 movers {+1:29, -1:32, -2:3}, the three -2 all Pacos/Moreirense CBs (teammates of recovered defenders). **8 band crossings, ALL INTERNAL, ZERO PUBLIC.** Flip rate 1.00, ripple share 27%.
    - **BLANK-TEAM GUARD SHIPPED + FIRST USE (commit d1e97ab).** A resolved row with a blank team_name degrades on 2 of 3 surfaces (rankings pills/compact leave an empty `.uclub` cell; card.html `glSub` renders a double " ·  · "; only rankings CARDS view filters empties via buildCard). Cause = live-season lag, a newly-promoted club (Estrela Amadora PRT 2025) whose name API-Football has not backfilled on the stat block , same family as the 25/26 dup-team issue. **Skipped, not inserted**, because it degrades on the premium surface AND under insert-only the skip SELF-HEALS (the row lands on a later pass once the source names the club; a blank card inserted now would be frozen). Dry run predicted 13 blank; live skipped **21** , the extra 8 appeared as the in-progress 2025 season crossed more promoted-club players over the 300-min floor between dry run and live. Both runs agree on the same 64 real cards. **Zero blank-team cards landed (verified).** The guard is GENERAL (all leagues, insert-only safe) , relevant because BL/ERE/BPL still have 2025 seasons ahead with the same lag risk.
    - **DEFERRED , the 21 blank-team PRT 2025 rows** (Estrela Amadora + other promoted clubs): re-run PRT insert-only once API-Football backfills the club names (weeks, as the season matures). All thin output, none near a band seam. Alternative recovery path if wanted sooner: resolve via `team.id` (present on the block even when `team.name` is null) -> `getOrCreateTeam` by id, needs a re-fetch since the id was not captured.
  - **BL SHIPPED 2026-07-23 (DB write, matview refreshed): 35 recovered, psc 5867 -> 5902, 0 errors.** Smallest league by count, most recent-skewed (83% 2020+). **Guirassy 2024 Dortmund 2602m 21g/2a -> rt86 WORLD CLASS** (proxy predicted 86, exact). Demirovic rt80 (proxy 80, exact). Proxy MAE **7.3, best of the rollout**.
    - **THE PREDICTED SECOND PUBLIC CROSSING DID NOT HAPPEN , informative, not proof of safety.** Guirassy entered a band with 77 incumbents; **all 66 BL cards sitting at rt83-86 HELD** (verified directly, not inferred). BL rt>=85 went 77->78 (Guirassy added, no incumbent lost). WHY: the anchor demotes a seam-sitter only when the b85 cutline (650th-best of ~53.6k) moves PAST it. Charles (LL) needed TWO cards landing exactly AT 85 to push the cutline past him; Guirassy is ONE card sitting ABOVE the seam at 86, so he barely moves the 650th-place cutline. **One elite card, even into a crowded band, is not enough , but n=1, do not generalise to "anchor is safe".**
    - 61 movers {+1:25, +2:1, -1:35}; the +2 is W. Anton, Guirassy's Dortmund CB teammate (largest team_def90 shift). 3 crossings ALL INTERNAL (57/58). **Flip rate 1.74 , HIGHEST of the rollout** (PL 1.57 prior high), because BL recovered several FULL-SEASON players (Guirassy 2602m, Nubel 2700m, Bischof 2567m) and high-minute recoveries perturb team_def90 hardest , textbook confirmation of "scales with minutes not row count".
    - **WATCH-LIST +2: BL Burke 2022** (Werder 254m/15ap 2g + Union 267m/18ap 2g, ratio 0.95, shared 2g) and **BL Scherhant 2022** (Hertha 279m/10ap 1g + Freiburg 287m/15ap 1g, ratio 0.97, shared 1g). Comparable minutes + shared stat below MIN_SHARED_STAT, no exact-minute equality so the mirror relaxation does not catch them. Class now: LL 0, L1 1, SA 3, TR 0 (Kapacak removed), PRT 0, BL 2 = 6 total live.
  - **POST-ROLLOUT FIX , COMBINE THE TWO GUARDS INTO A JOINT SCORE (logged 2026-07-22, DO NOT build mid-rollout).** [heading de-duplicated 2026-07-29 during the archive split , the original had this phrase pasted three times; body text untouched] The proportionality gate and the per-stat duplication guard are currently evaluated INDEPENDENTLY: each must clear its own threshold alone or nothing fires. **Being marginal on BOTH is itself evidence, and the current structure cannot express that.** MOTIVATING CASE , **TR B. Basacikoglu 2019** (Trabzonspor 72m/7ap 0g/**3a** + Kayserispor 483m/11ap 0g/**3a** -> sums to 555m 0g/6a, almost certainly a duplicated 3a): proportionality p=**0.0129** against a 0.01 gate, missing by 0.0029 (for scale Gibbs-White was 3.7e-5, Alex Sola 9.4e-4, Mama Balde 1.3e-3, all comfortably gated); AND guard (b)'s shared stat is 3a, EXACTLY meeting MIN_SHARED_STAT, but the minute ratio 72/483=0.15 fails the >=0.5 half. Two near-misses, no fire. PROPOSED SHAPE: replace the two independent gates with a single duplication SCORE combining (i) the minor block's proportionality tail, (ii) the shared-stat magnitude, (iii) the minute ratio, (iv) exact-minute equality as a strong term , and dedupe above a calibrated cut. **MEASURED SCOPE: exactly ONE row across all 5 captured leagues (239 multi-block rows) is marginal-on-both without firing , this one.** It is NOT a class, which is why it does not reorder the rollout. Revisit with all 9 leagues visible, alongside MIN_SHARED_STAT and the mirror Δapps tolerance.
  - **EXACT MINUTE EQUALITY IS A COPY SIGNATURE , measured 2026-07-22 across 239 multi-block rows in PL/LL/L1/SA/TR.** Only **3** block pairs anywhere share EXACTLY equal minutes, and **all 3 also share identical goals AND identical assists (3/3, perfect association)**. If exact-minute equality were coincidence it would be independent of output matching; it is not. The mirror test already encodes this idea but gates it behind `Δapps <= 1`, and **that tolerance is the thing failing** , not MIN_SHARED_STAT. **TR B. Kapacak 2022** (Fenerbahce 281m/**20ap** 1g/1a + Fatih Karagumruk 281m/**13ap** 1g/1a) escapes only because Δapps=7. **BLAST RADIUS of relaxing mirror to fire on exact minute equality regardless of apps: exactly 1 row (Kapacak). ZERO change in PL, LL, L1 or SA** , so unlike a MIN_SHARED_STAT change it does NOT break cross-league comparability and does NOT reopen any shipped row. Caveat kept on the record: n=3 is small, and differing apps (20 vs 13) is genuinely odd for a pure copy, so this is strong evidence rather than proof.
  - **TR SHIPPED 2026-07-22 (DB write, matview refreshed): 145 recovered, psc 6220 -> 6365, 0 errors.** Note **145, not the dry run's 146**: the mirror relaxation landed between dry run and live, so **B. Kapacak 2022 deduped to 281m , BELOW the 300-min floor , and produced no card at all.** The fix did not halve his output, it removed the row. 96 existing rows moved {+1:57, -1:38, **-2:1**}; **5 band crossings, ALL INTERNAL, ZERO PUBLIC**; mover rt range 22-70. Ripple share 19%. Flip rate 0.66/recovery. **0 new rows at rt>=85** (top is Nayir 2022 Konyaspor 2651m 17g/2a rt78, itself a 4-block artifact correctly deduped: two 0-min phantoms + two byte-identical real blocks, alphabetical tiebreak picking Konyaspor over Umraniyespor). **25 of 145 land unscored (null goals) , 17%, the highest unscored share of any league so far.**
    - **KNOWN-SUSPECT ROW SHIPPED AS AGREED: B. Basacikoglu 2019 Kayserispor 555m 0g/6a, rt50, card_id 187304.** Almost certainly a duplicated 3a (see the combined-score entry above). rt50 and nowhere near a band seam, so the cost of leaving it is negligible until the post-rollout review re-resolves it.
    - **OUTPUT PROXY , third league validated: MAE 7.4 across 120 scored recoveries** (L1 7.3, SA 8.6 / 8.4 outfield). Bias flipped direction on TR (over-predicts: Nayir +3, Akgun +4) where L1/SA under-predicted. Treat it as +-8 in the mid-range and reliable at the top end, not as a point estimate.

## 2.3 , SUPERSEDED PREDICTION , the rollout model (kept for the record)

This model projected ~3-5 public band crossings from ripple plus an uncaptured anchor effect, "order ~10" in total. The ACTUAL outcome across all 780 recoveries was **1** (Charles 2018 LL, 85 -> 84). The model was pessimistic: the estimators did not capture that a crossing requires a card landing AT a public seam, not merely above it (BL added Guirassy at rt86 into a band with 77 incumbents and produced zero crossings).

  - **ROLLOUT MODEL FOR THE 7 REMAINING LEAGUES (SA/BL/L1/PRT/ERE/BPL/TR).** Recovery rate 1.16% of rows (PL 67/6765 + LL 93/7032) -> **~497 more recoveries** (SA ~80, L1 ~76, TR ~72, PRT ~69, BL ~68, ERE ~67, BPL ~65) -> **~545 movers** at LL's 1.10 rate. Public crossings, two independent estimators that agree: (A) empirical 1 per 127 movers -> ~4.3; (B) boundary-exposure 1.15% of cards in the mover rt range x ~0.5 direction -> ~3.1. **So ~3-5 public crossings from the ripple.** BUT NEITHER ESTIMATOR MODELS THE ANCHOR MECHANISM, and Charles was an anchor casualty: every league that adds high-rt cards can demote a card sitting on 85/90/95 regardless of ripple. LL added 2 cards at rt85 and produced 1 demotion at 85. If that repeats per league, add up to ~7 more. **HONEST TOTAL: order 10 public crossings across the remaining rollout, wide error bars, concentrated at the 84/85 seam.**

**STALE "NEXT" , the rollout it points to is COMPLETE. Do not act on this line.**

  - NEXT: **SA next**, same gate every time (snapshot -> dry run -> report -> explicit go -> live -> refresh -> diff). Then BL/PRT/ERE/BPL/TR. Then the already-captured-wrong-block residual (Rashford stays Villa 441m under insert-only , needs a separate rt-touching pass, deferred).
